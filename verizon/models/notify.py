"""verizon.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501
from verizon.api_helper import APIHelper


class Notify(object):
    """Implementation of the 'Notify' model.

    Attributes:
        alert_type (str): The model property of type str.
        threshold (List[Carriercode1] | None): The model property of type
            List[Carriercode1] | None.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "alert_type": "alertType",
        "threshold": "threshold",
    }

    _optionals = [
        "alert_type",
        "threshold",
    ]

    def __init__(
        self,
        alert_type=APIHelper.SKIP,
        threshold=APIHelper.SKIP):
        """Initialize a Notify instance."""
        # Initialize members of the class
        if alert_type is not APIHelper.SKIP:
            self.alert_type = alert_type
        if threshold is not APIHelper.SKIP:
            self.threshold = threshold

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        from verizon.utilities.union_type_lookup import (
            UnionTypeLookUp,
        )

        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        alert_type =\
            dictionary.get("alertType")\
            if dictionary.get("alertType")\
                else APIHelper.SKIP
        threshold = APIHelper.deserialize_union_type(
            UnionTypeLookUp.get("NotifyThreshold"),
            dictionary.get("threshold"),
            False)\
            if dictionary.get("threshold") is not None\
            else APIHelper.SKIP

        # Return an object of this model
        return cls(alert_type,
                   threshold)

    @classmethod
    def validate(cls, dictionary):
        """Validate dictionary against class required properties

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            boolean : if dictionary is valid contains required properties.

        """
        if isinstance(dictionary, cls):
            return True

        if not isinstance(dictionary, dict):
            return False

        return True

    def __repr__(self):
        """Return a unambiguous string representation."""
        _alert_type=(
            self.alert_type
            if hasattr(self, "alert_type")
            else None
        )
        _threshold=(
            self.threshold
            if hasattr(self, "threshold")
            else None
        )
        return (
            f"{self.__class__.__name__}("
            f"alert_type={_alert_type!r}, "
            f"threshold={_threshold!r}, "
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _alert_type=(
            self.alert_type
            if hasattr(self, "alert_type")
            else None
        )
        _threshold=(
            self.threshold
            if hasattr(self, "threshold")
            else None
        )
        return (
            f"{self.__class__.__name__}("
            f"alert_type={_alert_type!s}, "
            f"threshold={_threshold!s}, "
            f")"
        )
