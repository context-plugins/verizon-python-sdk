"""verizon.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501
from verizon.api_helper import APIHelper
from verizon.models.denm_payload import DenmPayload
from verizon.models.header import Header


class EtsiAlertPayload(object):
    """Implementation of the 'EtsiAlertPayload' model.

    DENM (Decentralized Environmental Notification Message) payload as defined in
    ETSI.

    Attributes:
        header (Header): The header of the DENM PDU.
        denm (DenmPayload): The payload of the DENM PDU.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "header": "header",
        "denm": "denm",
    }

    def __init__(
        self,
        header=None,
        denm=None):
        """Initialize a EtsiAlertPayload instance."""
        # Initialize members of the class
        self.header = header
        self.denm = denm

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        header =\
            Header.from_dictionary(
                dictionary.get("header"))\
                if dictionary.get("header") else None
        denm =\
            DenmPayload.from_dictionary(
                dictionary.get("denm"))\
                if dictionary.get("denm") else None

        # Return an object of this model
        return cls(header,
                   denm)

    @classmethod
    def validate(cls, dictionary):
        """Validate dictionary against class required properties

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            boolean : if dictionary is valid contains required properties.

        """
        if isinstance(dictionary, cls):
            return APIHelper.is_valid_type(
                    value=dictionary.header,
                    type_callable=lambda value:
                        Header.validate(value),
                    is_model_dict=True) \
                and APIHelper.is_valid_type(
                    value=dictionary.denm,
                    type_callable=lambda value:
                        DenmPayload.validate(value),
                    is_model_dict=True)

        if not isinstance(dictionary, dict):
            return False

        return APIHelper.is_valid_type(
                value=dictionary.get("header"),
                type_callable=lambda value:
                    Header.validate(value),
                is_model_dict=True) \
            and APIHelper.is_valid_type(
                value=dictionary.get("denm"),
                type_callable=lambda value:
                    DenmPayload.validate(value),
                is_model_dict=True)

    def __repr__(self):
        """Return a unambiguous string representation."""
        _header=self.header
        _denm=self.denm
        return (
            f"{self.__class__.__name__}("
            f"header={_header!r}, "
            f"denm={_denm!r}, "
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _header=self.header
        _denm=self.denm
        return (
            f"{self.__class__.__name__}("
            f"header={_header!s}, "
            f"denm={_denm!s}, "
            f")"
        )
