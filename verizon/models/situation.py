"""verizon.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501
from verizon.api_helper import APIHelper
from verizon.models.event_type import EventType


class Situation(object):
    """Implementation of the 'Situation' model.

    This represents the situation container describing the event and the reliability
    of the detection source.

    Attributes:
        information_quality (int): The quality or reliability level of the
            information provided by the ITS-S application of the originating ITS-S.
        event_type (EventType): The type of event including direct and sub cause.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "information_quality": "informationQuality",
        "event_type": "eventType",
    }

    def __init__(
        self,
        information_quality=None,
        event_type=None):
        """Initialize a Situation instance."""
        # Initialize members of the class
        self.information_quality = information_quality
        self.event_type = event_type

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        information_quality =\
            dictionary.get("informationQuality")\
            if dictionary.get("informationQuality")\
                else None
        event_type =\
            EventType.from_dictionary(
                dictionary.get("eventType"))\
                if dictionary.get("eventType") else None

        # Return an object of this model
        return cls(information_quality,
                   event_type)

    @classmethod
    def validate(cls, dictionary):
        """Validate dictionary against class required properties

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            boolean : if dictionary is valid contains required properties.

        """
        if isinstance(dictionary, cls):
            return APIHelper.is_valid_type(
                    value=dictionary.information_quality,
                    type_callable=lambda value:
                        isinstance(
                        value,
                        int,
                )) \
                and APIHelper.is_valid_type(
                    value=dictionary.event_type,
                    type_callable=lambda value:
                        EventType.validate(value),
                    is_model_dict=True)

        if not isinstance(dictionary, dict):
            return False

        return APIHelper.is_valid_type(
                value=dictionary.get("informationQuality"),
                type_callable=lambda value:
                    isinstance(
                    value,
                    int,
            )) \
            and APIHelper.is_valid_type(
                value=dictionary.get("eventType"),
                type_callable=lambda value:
                    EventType.validate(value),
                is_model_dict=True)

    def __repr__(self):
        """Return a unambiguous string representation."""
        _information_quality=self.information_quality
        _event_type=self.event_type
        return (
            f"{self.__class__.__name__}("
            f"information_quality={_information_quality!r}, "
            f"event_type={_event_type!r}, "
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _information_quality=self.information_quality
        _event_type=self.event_type
        return (
            f"{self.__class__.__name__}("
            f"information_quality={_information_quality!s}, "
            f"event_type={_event_type!s}, "
            f")"
        )
