"""verizon.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501
from verizon.api_helper import APIHelper


class DevicesRequest(object):
    """Implementation of the 'DevicesRequest' model.

    Request body for retrieving devices based on vendorID and optional filters

    Attributes:
        vendor_id (str): The ID the vendor wants its devices to be registered under.
            E.g. Verizon, GM, Ford, etc.
        filter (DevicesFilter | PaginationFilter | None): Devices filter criteria or
            pagination token

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "vendor_id": "VendorId",
        "filter": "Filter",
    }

    _optionals = [
        "filter",
    ]

    def __init__(
        self,
        vendor_id=None,
        filter=APIHelper.SKIP):
        """Initialize a DevicesRequest instance."""
        # Initialize members of the class
        self.vendor_id = vendor_id
        if filter is not APIHelper.SKIP:
            self.filter = filter

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        from verizon.utilities.union_type_lookup import (
            UnionTypeLookUp,
        )

        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        vendor_id =\
            dictionary.get("VendorId")\
            if dictionary.get("VendorId")\
                else None
        filter = APIHelper.deserialize_union_type(
            UnionTypeLookUp.get("DevicesRequestFilter"),
            dictionary.get("Filter"),
            False)\
            if dictionary.get("Filter") is not None\
            else APIHelper.SKIP

        # Return an object of this model
        return cls(vendor_id,
                   filter)

    def __repr__(self):
        """Return a unambiguous string representation."""
        _vendor_id=self.vendor_id
        _filter=(
            self.filter
            if hasattr(self, "filter")
            else None
        )
        return (
            f"{self.__class__.__name__}("
            f"vendor_id={_vendor_id!r}, "
            f"filter={_filter!r}, "
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _vendor_id=self.vendor_id
        _filter=(
            self.filter
            if hasattr(self, "filter")
            else None
        )
        return (
            f"{self.__class__.__name__}("
            f"vendor_id={_vendor_id!s}, "
            f"filter={_filter!s}, "
            f")"
        )
