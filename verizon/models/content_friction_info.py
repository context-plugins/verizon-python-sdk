"""verizon.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501
from verizon.api_helper import APIHelper
from verizon.models.friction_information import (
    FrictionInformation,
)


class ContentFrictionInfo(object):
    """Implementation of the 'Content_frictionInfo' model.

    Attributes:
        friction_info (FrictionInformation): The model property of type
            FrictionInformation.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "friction_info": "frictionInfo",
    }

    def __init__(
        self,
        friction_info=None):
        """Initialize a ContentFrictionInfo instance."""
        # Initialize members of the class
        self.friction_info = friction_info

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        friction_info =\
            FrictionInformation.from_dictionary(
                dictionary.get("frictionInfo"))\
                if dictionary.get("frictionInfo") else None

        # Return an object of this model
        return cls(friction_info)

    @classmethod
    def validate(cls, dictionary):
        """Validate dictionary against class required properties

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            boolean : if dictionary is valid contains required properties.

        """
        if isinstance(dictionary, cls):
            return APIHelper.is_valid_type(
                    value=dictionary.friction_info,
                    type_callable=lambda value:
                        FrictionInformation.validate(value),
                    is_model_dict=True)

        if not isinstance(dictionary, dict):
            return False

        return APIHelper.is_valid_type(
                value=dictionary.get("frictionInfo"),
                type_callable=lambda value:
                    FrictionInformation.validate(value),
                is_model_dict=True)

    def __repr__(self):
        """Return a unambiguous string representation."""
        _friction_info=self.friction_info
        return (
            f"{self.__class__.__name__}("
            f"friction_info={_friction_info!r}, "
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _friction_info=self.friction_info
        return (
            f"{self.__class__.__name__}("
            f"friction_info={_friction_info!s}, "
            f")"
        )
