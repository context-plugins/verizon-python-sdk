"""verizon.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501
from verizon.api_helper import APIHelper
from verizon.models.speed_range import SpeedRange


class SpeedItem(object):
    """Implementation of the 'SpeedItem' model.

    Defines the acceptable speed range for road users in m/s. Messages are triggered
    when:
        1. The road user's speed is below the required minimum OR
        2. The road user's speed is above the acceptable maximum AND
        3. The associated TriggerConditions are met.
    Example: For the speed range of 10-20 m/s and a TriggerCondition of 'user inside
    geofence', the message is sent if the user's speed is below 10 m/s or above 20
    m/s while in the geofence area.

    Attributes:
        speed (SpeedRange): Acceptable speed range for road users in m/s.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "speed": "speed",
    }

    _nullables = [
        "speed",
    ]

    def __init__(
        self,
        speed=None):
        """Initialize a SpeedItem instance."""
        # Initialize members of the class
        self.speed = speed

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        speed =\
            SpeedRange.from_dictionary(
                dictionary.get("speed"))\
                if dictionary.get("speed") else None

        # Return an object of this model
        return cls(speed)

    @classmethod
    def validate(cls, dictionary):
        """Validate dictionary against class required properties

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            boolean : if dictionary is valid contains required properties.

        """
        if isinstance(dictionary, cls):
            return APIHelper.is_valid_type(
                    value=dictionary.speed,
                    type_callable=lambda value:
                        SpeedRange.validate(value),
                    is_value_nullable=True,
                    is_model_dict=True)

        if not isinstance(dictionary, dict):
            return False

        return APIHelper.is_valid_type(
                value=dictionary.get("speed"),
                type_callable=lambda value:
                    SpeedRange.validate(value),
                is_value_nullable=True,
                is_model_dict=True)

    def __repr__(self):
        """Return a unambiguous string representation."""
        _speed=self.speed
        return (
            f"{self.__class__.__name__}("
            f"speed={_speed!r}, "
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _speed=self.speed
        return (
            f"{self.__class__.__name__}("
            f"speed={_speed!s}, "
            f")"
        )
