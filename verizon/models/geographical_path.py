"""verizon.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501
from verizon.api_helper import APIHelper
from verizon.models.geographical_path_description import (
    GeographicalPathDescription,
)


class GeographicalPath(object):
    """Implementation of the 'GeographicalPath' model.

    The data frame is used to support the cross-cutting need in many V2X messages to
    describe arbitrary spatial areas (polygons, boundary lines, and other basic
    shapes) required by various message types in a small message size.

    Attributes:
        description (GeographicalPathDescription): This data frame can describe a
            complex path of arbitrary size using node offset method (LL offsets).
        direction (str): OctetStrings are described as hexadecimal strings, where
            each octet is represented by two hexadecimal characters.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "description": "description",
        "direction": "direction",
    }

    _optionals = [
        "description",
        "direction",
    ]

    def __init__(
        self,
        description=APIHelper.SKIP,
        direction=APIHelper.SKIP):
        """Initialize a GeographicalPath instance."""
        # Initialize members of the class
        if description is not APIHelper.SKIP:
            self.description = description
        if direction is not APIHelper.SKIP:
            self.direction = direction

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        description =\
            GeographicalPathDescription.from_dictionary(
                dictionary.get("description"))\
                if "description" in dictionary.keys()\
                else APIHelper.SKIP
        direction =\
            dictionary.get("direction")\
            if dictionary.get("direction")\
                else APIHelper.SKIP

        # Return an object of this model
        return cls(description,
                   direction)

    @classmethod
    def validate(cls, dictionary):
        """Validate dictionary against class required properties

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            boolean : if dictionary is valid contains required properties.

        """
        if isinstance(dictionary, cls):
            return True

        if not isinstance(dictionary, dict):
            return False

        return True

    def __repr__(self):
        """Return a unambiguous string representation."""
        _description=(
            self.description
            if hasattr(self, "description")
            else None
        )
        _direction=(
            self.direction
            if hasattr(self, "direction")
            else None
        )
        return (
            f"{self.__class__.__name__}("
            f"description={_description!r}, "
            f"direction={_direction!r}, "
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _description=(
            self.description
            if hasattr(self, "description")
            else None
        )
        _direction=(
            self.direction
            if hasattr(self, "direction")
            else None
        )
        return (
            f"{self.__class__.__name__}("
            f"description={_description!s}, "
            f"direction={_direction!s}, "
            f")"
        )
