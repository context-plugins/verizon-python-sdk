"""verizon.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501
from verizon.api_helper import APIHelper
from verizon.models.road_sign_position import (
    RoadSignPosition,
)


class RoadSignID(object):
    """Implementation of the 'RoadSignID' model.

    It provide a precise location of one or more roadside signs.

    Attributes:
        position (RoadSignPosition): Precise location of a road sign in the WGS-84
            coordinate system, from which short offsets may be used to create
            additional data using a flat earth projection centered on this location.
        view_angle (str): OctetStrings are described as hexadecimal strings, where
            each octet is represented by two hexadecimal characters.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "position": "position",
        "view_angle": "viewAngle",
    }

    def __init__(
        self,
        position=None,
        view_angle=None):
        """Initialize a RoadSignID instance."""
        # Initialize members of the class
        self.position = position
        self.view_angle = view_angle

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        position =\
            RoadSignPosition.from_dictionary(
                dictionary.get("position"))\
                if dictionary.get("position") else None
        view_angle =\
            dictionary.get("viewAngle")\
            if dictionary.get("viewAngle")\
                else None

        # Return an object of this model
        return cls(position,
                   view_angle)

    @classmethod
    def validate(cls, dictionary):
        """Validate dictionary against class required properties

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            boolean : if dictionary is valid contains required properties.

        """
        if isinstance(dictionary, cls):
            return APIHelper.is_valid_type(
                    value=dictionary.position,
                    type_callable=lambda value:
                        RoadSignPosition.validate(value),
                    is_model_dict=True) \
                and APIHelper.is_valid_type(
                    value=dictionary.view_angle,
                    type_callable=lambda value:
                        isinstance(
                        value,
                        str,
                ))

        if not isinstance(dictionary, dict):
            return False

        return APIHelper.is_valid_type(
                value=dictionary.get("position"),
                type_callable=lambda value:
                    RoadSignPosition.validate(value),
                is_model_dict=True) \
            and APIHelper.is_valid_type(
                value=dictionary.get("viewAngle"),
                type_callable=lambda value:
                    isinstance(
                    value,
                    str,
            ))

    def __repr__(self):
        """Return a unambiguous string representation."""
        _position=self.position
        _view_angle=self.view_angle
        return (
            f"{self.__class__.__name__}("
            f"position={_position!r}, "
            f"view_angle={_view_angle!r}, "
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _position=self.position
        _view_angle=self.view_angle
        return (
            f"{self.__class__.__name__}("
            f"position={_position!s}, "
            f"view_angle={_view_angle!s}, "
            f")"
        )
