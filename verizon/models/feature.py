"""verizon.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501
from verizon.api_helper import APIHelper


class Feature(object):
    """Implementation of the 'Feature' model.

    Attributes:
        features (str): The calling and data features available for the account.
            **Note:** for Global IoT Orchestrator, the features `eUICC Verizon as
            Lead` and `Global eSim Billing` will always be present.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "features": "features",
    }

    _optionals = [
        "features",
    ]

    def __init__(
        self,
        features=APIHelper.SKIP):
        """Initialize a Feature instance."""
        # Initialize members of the class
        if features is not APIHelper.SKIP:
            self.features = features

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        features =\
            dictionary.get("features")\
            if dictionary.get("features")\
                else APIHelper.SKIP

        # Return an object of this model
        return cls(features)

    def __repr__(self):
        """Return a unambiguous string representation."""
        _features=(
            self.features
            if hasattr(self, "features")
            else None
        )
        return (
            f"{self.__class__.__name__}("
            f"features={_features!r}, "
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _features=(
            self.features
            if hasattr(self, "features")
            else None
        )
        return (
            f"{self.__class__.__name__}("
            f"features={_features!s}, "
            f")"
        )
