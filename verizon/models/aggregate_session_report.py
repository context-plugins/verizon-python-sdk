"""verizon.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501
from verizon.api_helper import APIHelper
from verizon.models.aggregate_usage_error import (
    AggregateUsageError,
)
from verizon.models.aggregate_usage_item import (
    AggregateUsageItem,
)


class AggregateSessionReport(object):
    """Implementation of the 'AggregateSessionReport' model.

    Session and usage details for up to 10 devices.

    Attributes:
        txid (str): A unique string (UUID) that associates the request with the
            location report information that is sent in asynchronous callback
            message.ThingSpace will send a separate callback message for each device
            that was in the request. All of the callback messages will have a txid.
        usage (List[AggregateUsageItem]): Contains usage per device.
        errors (List[AggregateUsageError]): An object containing any errors reported
            by the device.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "txid": "txid",
        "usage": "usage",
        "errors": "errors",
    }

    _optionals = [
        "txid",
        "usage",
        "errors",
    ]

    def __init__(
        self,
        txid=APIHelper.SKIP,
        usage=APIHelper.SKIP,
        errors=APIHelper.SKIP):
        """Initialize a AggregateSessionReport instance."""
        # Initialize members of the class
        if txid is not APIHelper.SKIP:
            self.txid = txid
        if usage is not APIHelper.SKIP:
            self.usage = usage
        if errors is not APIHelper.SKIP:
            self.errors = errors

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        txid =\
            dictionary.get("txid")\
            if dictionary.get("txid")\
                else APIHelper.SKIP
        usage = None
        if dictionary.get("usage") is not None:
            usage = [
                AggregateUsageItem.from_dictionary(x)
                    for x in dictionary.get("usage")
            ]
        else:
            usage = APIHelper.SKIP
        errors = None
        if dictionary.get("errors") is not None:
            errors = [
                AggregateUsageError.from_dictionary(x)
                    for x in dictionary.get("errors")
            ]
        else:
            errors = APIHelper.SKIP

        # Return an object of this model
        return cls(txid,
                   usage,
                   errors)

    def __repr__(self):
        """Return a unambiguous string representation."""
        _txid=(
            self.txid
            if hasattr(self, "txid")
            else None
        )
        _usage=(
            self.usage
            if hasattr(self, "usage")
            else None
        )
        _errors=(
            self.errors
            if hasattr(self, "errors")
            else None
        )
        return (
            f"{self.__class__.__name__}("
            f"txid={_txid!r}, "
            f"usage={_usage!r}, "
            f"errors={_errors!r}, "
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _txid=(
            self.txid
            if hasattr(self, "txid")
            else None
        )
        _usage=(
            self.usage
            if hasattr(self, "usage")
            else None
        )
        _errors=(
            self.errors
            if hasattr(self, "errors")
            else None
        )
        return (
            f"{self.__class__.__name__}("
            f"txid={_txid!s}, "
            f"usage={_usage!s}, "
            f"errors={_errors!s}, "
            f")"
        )
