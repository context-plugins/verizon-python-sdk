"""verizon.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501
from verizon.api_helper import APIHelper
from verizon.models.account_level_object import (
    AccountLevelObject,
)


class DataTrigger(object):
    """Implementation of the 'DataTrigger' model.

    Attributes:
        account_level (AccountLevelObject): The model property of type
            AccountLevelObject.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "account_level": "accountLevel",
    }

    _optionals = [
        "account_level",
    ]

    def __init__(
        self,
        account_level=APIHelper.SKIP):
        """Initialize a DataTrigger instance."""
        # Initialize members of the class
        if account_level is not APIHelper.SKIP:
            self.account_level = account_level

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        account_level =\
            AccountLevelObject.from_dictionary(
                dictionary.get("accountLevel"))\
                if "accountLevel" in dictionary.keys()\
                else APIHelper.SKIP

        # Return an object of this model
        return cls(account_level)

    @classmethod
    def validate(cls, dictionary):
        """Validate dictionary against class required properties

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            boolean : if dictionary is valid contains required properties.

        """
        if isinstance(dictionary, cls):
            return True

        if not isinstance(dictionary, dict):
            return False

        return True

    def __repr__(self):
        """Return a unambiguous string representation."""
        _account_level=(
            self.account_level
            if hasattr(self, "account_level")
            else None
        )
        return (
            f"{self.__class__.__name__}("
            f"account_level={_account_level!r}, "
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _account_level=(
            self.account_level
            if hasattr(self, "account_level")
            else None
        )
        return (
            f"{self.__class__.__name__}("
            f"account_level={_account_level!s}, "
            f")"
        )
