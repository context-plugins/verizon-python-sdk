"""verizon.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501
from verizon.api_helper import APIHelper
from verizon.models.accountnames import Accountnames


class AccountLevelFilter(object):
    """Implementation of the 'accountLevelFilter' model.

    Attributes:
        separate_or_combined (str): Determines whether or not to aggregate usage of
            multiple accounts together, or separate by account. If this is null or
            not present, then the trigger will be for an individual line.
        account_names (Accountnames): The model property of type Accountnames.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "separate_or_combined": "separateOrCombined",
        "account_names": "accountNames",
    }

    _optionals = [
        "separate_or_combined",
        "account_names",
    ]

    def __init__(
        self,
        separate_or_combined=APIHelper.SKIP,
        account_names=APIHelper.SKIP):
        """Initialize a AccountLevelFilter instance."""
        # Initialize members of the class
        if separate_or_combined is not APIHelper.SKIP:
            self.separate_or_combined = separate_or_combined
        if account_names is not APIHelper.SKIP:
            self.account_names = account_names

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        separate_or_combined =\
            dictionary.get("separateOrCombined")\
            if dictionary.get("separateOrCombined")\
                else APIHelper.SKIP
        account_names =\
            Accountnames.from_dictionary(
                dictionary.get("accountNames"))\
                if "accountNames" in dictionary.keys()\
                else APIHelper.SKIP

        # Return an object of this model
        return cls(separate_or_combined,
                   account_names)

    @classmethod
    def validate(cls, dictionary):
        """Validate dictionary against class required properties

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            boolean : if dictionary is valid contains required properties.

        """
        if isinstance(dictionary, cls):
            return True

        if not isinstance(dictionary, dict):
            return False

        return True

    def __repr__(self):
        """Return a unambiguous string representation."""
        _separate_or_combined=(
            self.separate_or_combined
            if hasattr(self, "separate_or_combined")
            else None
        )
        _account_names=(
            self.account_names
            if hasattr(self, "account_names")
            else None
        )
        return (
            f"{self.__class__.__name__}("
            f"separate_or_combined={_separate_or_combined!r}, "
            f"account_names={_account_names!r}, "
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _separate_or_combined=(
            self.separate_or_combined
            if hasattr(self, "separate_or_combined")
            else None
        )
        _account_names=(
            self.account_names
            if hasattr(self, "account_names")
            else None
        )
        return (
            f"{self.__class__.__name__}("
            f"separate_or_combined={_separate_or_combined!s}, "
            f"account_names={_account_names!s}, "
            f")"
        )
