"""verizon.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501
from verizon.api_helper import APIHelper
from verizon.models.management import Management
from verizon.models.situation import Situation


class DenmPayload(object):
    """Implementation of the 'DenmPayload' model.

    The payload of the DENM PDU.

    Attributes:
        management (Management): This represent the management container describing
            the meta information about the event, such as the detection time, the
            event's location, the source of the event, and the notification distance.
        situation (Situation): This represents the situation container describing the
            event and the reliability of the detection source.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "management": "management",
        "situation": "situation",
    }

    _optionals = [
        "situation",
    ]

    def __init__(
        self,
        management=None,
        situation=APIHelper.SKIP):
        """Initialize a DenmPayload instance."""
        # Initialize members of the class
        self.management = management
        if situation is not APIHelper.SKIP:
            self.situation = situation

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        management =\
            Management.from_dictionary(
                dictionary.get("management"))\
                if dictionary.get("management") else None
        situation =\
            Situation.from_dictionary(
                dictionary.get("situation"))\
                if "situation" in dictionary.keys()\
                else APIHelper.SKIP

        # Return an object of this model
        return cls(management,
                   situation)

    @classmethod
    def validate(cls, dictionary):
        """Validate dictionary against class required properties

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            boolean : if dictionary is valid contains required properties.

        """
        if isinstance(dictionary, cls):
            return APIHelper.is_valid_type(
                    value=dictionary.management,
                    type_callable=lambda value:
                        Management.validate(value),
                    is_model_dict=True)

        if not isinstance(dictionary, dict):
            return False

        return APIHelper.is_valid_type(
                value=dictionary.get("management"),
                type_callable=lambda value:
                    Management.validate(value),
                is_model_dict=True)

    def __repr__(self):
        """Return a unambiguous string representation."""
        _management=self.management
        _situation=(
            self.situation
            if hasattr(self, "situation")
            else None
        )
        return (
            f"{self.__class__.__name__}("
            f"management={_management!r}, "
            f"situation={_situation!r}, "
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _management=self.management
        _situation=(
            self.situation
            if hasattr(self, "situation")
            else None
        )
        return (
            f"{self.__class__.__name__}("
            f"management={_management!s}, "
            f"situation={_situation!s}, "
            f")"
        )
