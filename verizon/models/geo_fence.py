"""verizon.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501
from verizon.api_helper import APIHelper
from verizon.models.feature_item import FeatureItem
from verizon.models.type_enum import TypeEnum


class GeoFence(object):
    """Implementation of the 'GeoFence' model.

    The GeoJSON representation of geofence. Geofence supports the following geometry
    types: LineString, Polygon, MultiLineString, and MultiPolygon. The system only
    supports a single Feature in the FeatureCollection, so only one Line, Polygon,
    MultiLine or MultiPolygon can be defined within one Geofencing configuration.

    Attributes:
        mtype (TypeEnum): The model property of type TypeEnum.
        features (List[FeatureItem]): The model property of type List[FeatureItem].

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "mtype": "type",
        "features": "features",
    }

    def __init__(
        self,
        mtype=None,
        features=None):
        """Initialize a GeoFence instance."""
        # Initialize members of the class
        self.mtype = mtype
        self.features = features

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        mtype =\
            dictionary.get("type")\
            if dictionary.get("type")\
                else None
        features = None
        if dictionary.get("features") is not None:
            features = [
                FeatureItem.from_dictionary(x)
                    for x in dictionary.get("features")
            ]

        # Return an object of this model
        return cls(mtype,
                   features)

    @classmethod
    def validate(cls, dictionary):
        """Validate dictionary against class required properties

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            boolean : if dictionary is valid contains required properties.

        """
        if isinstance(dictionary, cls):
            return APIHelper.is_valid_type(
                    value=dictionary.mtype,
                    type_callable=lambda value:
                        TypeEnum.validate(value)) \
                and APIHelper.is_valid_type(
                    value=dictionary.features,
                    type_callable=lambda value:
                        FeatureItem.validate(value),
                    is_model_dict=True,
                    is_inner_model_dict=True)

        if not isinstance(dictionary, dict):
            return False

        return APIHelper.is_valid_type(
                value=dictionary.get("type"),
                type_callable=lambda value:
                    TypeEnum.validate(value)) \
            and APIHelper.is_valid_type(
                value=dictionary.get("features"),
                type_callable=lambda value:
                    FeatureItem.validate(value),
                is_model_dict=True,
                is_inner_model_dict=True)

    def __repr__(self):
        """Return a unambiguous string representation."""
        _mtype=self.mtype
        _features=self.features
        return (
            f"{self.__class__.__name__}("
            f"mtype={_mtype!r}, "
            f"features={_features!r}, "
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _mtype=self.mtype
        _features=self.features
        return (
            f"{self.__class__.__name__}("
            f"mtype={_mtype!s}, "
            f"features={_features!s}, "
            f")"
        )
