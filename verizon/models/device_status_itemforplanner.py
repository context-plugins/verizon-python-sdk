"""verizon.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501
from verizon.api_helper import APIHelper
from verizon.models.device_idforplanner import (
    DeviceIdforplanner,
)


class DeviceStatusItemforplanner(object):
    """Implementation of the 'DeviceStatusItemforplanner' model.

    Attributes:
        device_ids (List[DeviceIdforplanner]): The model property of type
            List[DeviceIdforplanner].
        status (str): The model property of type str.
        reason (str): The model property of type str.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "device_ids": "deviceIds",
        "status": "status",
        "reason": "reason",
    }

    _optionals = [
        "device_ids",
        "status",
        "reason",
    ]

    _nullables = [
        "device_ids",
        "status",
        "reason",
    ]

    def __init__(
        self,
        device_ids=APIHelper.SKIP,
        status=APIHelper.SKIP,
        reason=APIHelper.SKIP):
        """Initialize a DeviceStatusItemforplanner instance."""
        # Initialize members of the class
        if device_ids is not APIHelper.SKIP:
            self.device_ids = device_ids
        if status is not APIHelper.SKIP:
            self.status = status
        if reason is not APIHelper.SKIP:
            self.reason = reason

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        if "deviceIds" in dictionary.keys():
            device_ids = [
                DeviceIdforplanner.from_dictionary(x)
                for x in dictionary.get("deviceIds")
            ] if dictionary.get("deviceIds") else None
        else:
            device_ids = APIHelper.SKIP
        status =\
            dictionary.get("status")\
            if "status" in dictionary.keys()\
                else APIHelper.SKIP
        reason =\
            dictionary.get("reason")\
            if "reason" in dictionary.keys()\
                else APIHelper.SKIP

        # Return an object of this model
        return cls(device_ids,
                   status,
                   reason)

    def __repr__(self):
        """Return a unambiguous string representation."""
        _device_ids=(
            self.device_ids
            if hasattr(self, "device_ids")
            else None
        )
        _status=(
            self.status
            if hasattr(self, "status")
            else None
        )
        _reason=(
            self.reason
            if hasattr(self, "reason")
            else None
        )
        return (
            f"{self.__class__.__name__}("
            f"device_ids={_device_ids!r}, "
            f"status={_status!r}, "
            f"reason={_reason!r}, "
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _device_ids=(
            self.device_ids
            if hasattr(self, "device_ids")
            else None
        )
        _status=(
            self.status
            if hasattr(self, "status")
            else None
        )
        _reason=(
            self.reason
            if hasattr(self, "reason")
            else None
        )
        return (
            f"{self.__class__.__name__}("
            f"device_ids={_device_ids!s}, "
            f"status={_status!s}, "
            f"reason={_reason!s}, "
            f")"
        )
