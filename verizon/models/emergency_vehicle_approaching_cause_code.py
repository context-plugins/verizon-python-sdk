"""verizon.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501
from verizon.api_helper import APIHelper


class EmergencyVehicleApproachingCauseCode(object):
    """Implementation of the 'EmergencyVehicleApproachingCauseCode' model.

    Cause code wrapper for emergency vehicle approaching events.

    Attributes:
        emergency_vehicle_approaching_95 (int): The value shall be set to: - 0
            `unavailable`                   - in case further detailed information on
            the emergency vehicle approaching event is unavailable, - 1
            `emergencyVehicleApproaching`   - in case an operating emergency vehicle
            is approaching, - 2 `prioritizedVehicleApproaching` - in case a
            prioritized vehicle is approaching, - 3-255                             -
            reserved for future usage.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "emergency_vehicle_approaching_95": "emergencyVehicleApproaching95",
    }

    def __init__(
        self,
        emergency_vehicle_approaching_95=None):
        """Initialize a EmergencyVehicleApproachingCauseCode instance."""
        # Initialize members of the class
        self.emergency_vehicle_approaching_95 = emergency_vehicle_approaching_95

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        emergency_vehicle_approaching_95 =\
            dictionary.get("emergencyVehicleApproaching95")\
            if dictionary.get("emergencyVehicleApproaching95")\
                else None

        # Return an object of this model
        return cls(emergency_vehicle_approaching_95)

    @classmethod
    def validate(cls, dictionary):
        """Validate dictionary against class required properties

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            boolean : if dictionary is valid contains required properties.

        """
        if isinstance(dictionary, cls):
            return APIHelper.is_valid_type(
                    value=dictionary.emergency_vehicle_approaching_95,
                    type_callable=lambda value:
                        isinstance(
                        value,
                        int,
                ))

        if not isinstance(dictionary, dict):
            return False

        return APIHelper.is_valid_type(
                value=dictionary.get("emergencyVehicleApproaching95"),
                type_callable=lambda value:
                    isinstance(
                    value,
                    int,
            ))

    def __repr__(self):
        """Return a unambiguous string representation."""
        _emergency_vehicle_approaching_95=self.emergency_vehicle_approaching_95
        return (
            f"{self.__class__.__name__}("
            f"emergency_vehicle_approaching_95={_emergency_vehicle_approaching_95!r}, "
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _emergency_vehicle_approaching_95=self.emergency_vehicle_approaching_95
        return (
            f"{self.__class__.__name__}("
            f"emergency_vehicle_approaching_95={_emergency_vehicle_approaching_95!s}, "
            f")"
        )
