"""verizon.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501
from verizon.api_helper import APIHelper


class CallbackCreated(object):
    """Implementation of the 'CallbackCreated' model.

    Attributes:
        account_name (str): The numeric name of the account and must include leading
            zeroes.
        name (str): The name of the callback service, which identifies the type and
            format of messages that will be sent to the registered URL.
        url (str): The address of the callback listening service where the ThingSpace
            Platform will send callback messages for the service type.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "account_name": "accountName",
        "name": "name",
        "url": "url",
    }

    _optionals = [
        "url",
    ]

    def __init__(
        self,
        account_name=None,
        name=None,
        url=APIHelper.SKIP):
        """Initialize a CallbackCreated instance."""
        # Initialize members of the class
        self.account_name = account_name
        self.name = name
        if url is not APIHelper.SKIP:
            self.url = url

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        account_name =\
            dictionary.get("accountName")\
            if dictionary.get("accountName")\
                else None
        name =\
            dictionary.get("name")\
            if dictionary.get("name")\
                else None
        url =\
            dictionary.get("url")\
            if dictionary.get("url")\
                else APIHelper.SKIP

        # Return an object of this model
        return cls(account_name,
                   name,
                   url)

    def __repr__(self):
        """Return a unambiguous string representation."""
        _account_name=self.account_name
        _name=self.name
        _url=(
            self.url
            if hasattr(self, "url")
            else None
        )
        return (
            f"{self.__class__.__name__}("
            f"account_name={_account_name!r}, "
            f"name={_name!r}, "
            f"url={_url!r}, "
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _account_name=self.account_name
        _name=self.name
        _url=(
            self.url
            if hasattr(self, "url")
            else None
        )
        return (
            f"{self.__class__.__name__}("
            f"account_name={_account_name!s}, "
            f"name={_name!s}, "
            f"url={_url!s}, "
            f")"
        )
