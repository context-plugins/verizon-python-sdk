"""verizon.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501
from verizon.api_helper import APIHelper


class GeofencePolygon(object):
    """Implementation of the 'GeofencePolygon' model.

    GeoJSON Polygon geofence object.

    Attributes:
        mtype (ETXMAPMessageGeofenceGeometryEnum): Type of the GeoJSON geometry, must
            be 'Polygon'.
        coordinates (List[float]): Coordinates of the GeoJSON polygon. Rules for a
            valid GeoJSON Polygon: - Must contain between 4 and 50 points. - Must be
            an array of linear rings (arrays of positions). - The first linear ring
            represents the outer boundary; subsequent rings would represent holes.
            Holes are not supported, so only one linear ring should be defined in a
            polygon. - Each linear ring must have at least 4 positions, and the first
            and last positions must be identical to close the polygon ring. - A
            linear ring must follow the right-hand rule with respect to the area it
            bounds: exterior rings (outer boundery) are counterclockwise. - Each
            position is an array of two numbers: [longitude, latitude]. - Longitude
            and latitude values must be in decimal degrees. - Longitude must be
            ranging from -180 to 180. - Latitude must be ranging from -90 to 90. -
            The polygon must not self-intersect.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "mtype": "type",
        "coordinates": "coordinates",
    }

    _optionals = [
        "mtype",
        "coordinates",
    ]

    def __init__(
        self,
        mtype=APIHelper.SKIP,
        coordinates=APIHelper.SKIP):
        """Initialize a GeofencePolygon instance."""
        # Initialize members of the class
        if mtype is not APIHelper.SKIP:
            self.mtype = mtype
        if coordinates is not APIHelper.SKIP:
            self.coordinates = coordinates

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        mtype =\
            dictionary.get("type")\
            if dictionary.get("type")\
                else APIHelper.SKIP
        coordinates =\
            dictionary.get("coordinates")\
            if dictionary.get("coordinates")\
                else APIHelper.SKIP

        # Return an object of this model
        return cls(mtype,
                   coordinates)

    def __repr__(self):
        """Return a unambiguous string representation."""
        _mtype=(
            self.mtype
            if hasattr(self, "mtype")
            else None
        )
        _coordinates=(
            self.coordinates
            if hasattr(self, "coordinates")
            else None
        )
        return (
            f"{self.__class__.__name__}("
            f"mtype={_mtype!r}, "
            f"coordinates={_coordinates!r}, "
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _mtype=(
            self.mtype
            if hasattr(self, "mtype")
            else None
        )
        _coordinates=(
            self.coordinates
            if hasattr(self, "coordinates")
            else None
        )
        return (
            f"{self.__class__.__name__}("
            f"mtype={_mtype!s}, "
            f"coordinates={_coordinates!s}, "
            f")"
        )
