"""verizon.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501
from verizon.api_helper import APIHelper


class SaeAlertPayload(object):
    """Implementation of the 'SaeAlertPayload' model.

    Road Side Alert (RSA) message payload as defined in SAE J2735.

    Attributes:
        msg_cnt (int): It is used to provide a sequence number within a stream of
            messages with the same DSRCmsgID (here RoadSideAlert) and from the same
            sender.
        type_event (int): The ITIS Code that describes the alert/danger/hazard. All
            ITS standards use the same types here to explain the type of the
            alert/danger/hazard involved.  The complete set of ITIS codes can be
            found in Volume Two of the SAE J2540 standard. This is a set of over 1000
            items which are used to encode common events and list items in ITS.
        description (List[int]): ITIS code set entries to further describe the event,
            give advice, or any other ITIS codes related to the event/danger/hazard.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "type_event": "typeEvent",
        "msg_cnt": "msgCnt",
        "description": "description",
    }

    _optionals = [
        "msg_cnt",
        "description",
    ]

    def __init__(
        self,
        type_event=None,
        msg_cnt=0,
        description=APIHelper.SKIP):
        """Initialize a SaeAlertPayload instance."""
        # Initialize members of the class
        self.msg_cnt = msg_cnt
        self.type_event = type_event
        if description is not APIHelper.SKIP:
            self.description = description

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        type_event =\
            dictionary.get("typeEvent")\
            if dictionary.get("typeEvent")\
                else None
        msg_cnt =\
            dictionary.get("msgCnt")\
            if dictionary.get("msgCnt")\
                else 0
        description =\
            dictionary.get("description")\
            if dictionary.get("description")\
                else APIHelper.SKIP

        # Return an object of this model
        return cls(type_event,
                   msg_cnt,
                   description)

    @classmethod
    def validate(cls, dictionary):
        """Validate dictionary against class required properties

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            boolean : if dictionary is valid contains required properties.

        """
        if isinstance(dictionary, cls):
            return APIHelper.is_valid_type(
                    value=dictionary.type_event,
                    type_callable=lambda value:
                        isinstance(
                        value,
                        int,
                ))

        if not isinstance(dictionary, dict):
            return False

        return APIHelper.is_valid_type(
                value=dictionary.get("typeEvent"),
                type_callable=lambda value:
                    isinstance(
                    value,
                    int,
            ))

    def __repr__(self):
        """Return a unambiguous string representation."""
        _msg_cnt=(
            self.msg_cnt
            if hasattr(self, "msg_cnt")
            else None
        )
        _type_event=self.type_event
        _description=(
            self.description
            if hasattr(self, "description")
            else None
        )
        return (
            f"{self.__class__.__name__}("
            f"msg_cnt={_msg_cnt!r}, "
            f"type_event={_type_event!r}, "
            f"description={_description!r}, "
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _msg_cnt=(
            self.msg_cnt
            if hasattr(self, "msg_cnt")
            else None
        )
        _type_event=self.type_event
        _description=(
            self.description
            if hasattr(self, "description")
            else None
        )
        return (
            f"{self.__class__.__name__}("
            f"msg_cnt={_msg_cnt!s}, "
            f"type_event={_type_event!s}, "
            f"description={_description!s}, "
            f")"
        )
