"""verizon.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501
from verizon.api_helper import APIHelper


class ActionId(object):
    """Implementation of the 'ActionId' model.

    Attributes:
        originating_station_id (int): Unique ID for originating station.
        sequence_number (int): Counter used to differenciate multiple DENMs from same
            station.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "originating_station_id": "originatingStationId",
        "sequence_number": "sequenceNumber",
    }

    def __init__(
        self,
        originating_station_id=None,
        sequence_number=None):
        """Initialize a ActionId instance."""
        # Initialize members of the class
        self.originating_station_id = originating_station_id
        self.sequence_number = sequence_number

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        originating_station_id =\
            dictionary.get("originatingStationId")\
            if dictionary.get("originatingStationId")\
                else None
        sequence_number =\
            dictionary.get("sequenceNumber")\
            if dictionary.get("sequenceNumber")\
                else None

        # Return an object of this model
        return cls(originating_station_id,
                   sequence_number)

    @classmethod
    def validate(cls, dictionary):
        """Validate dictionary against class required properties

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            boolean : if dictionary is valid contains required properties.

        """
        if isinstance(dictionary, cls):
            return APIHelper.is_valid_type(
                    value=dictionary.originating_station_id,
                    type_callable=lambda value:
                        isinstance(
                        value,
                        int,
                )) \
                and APIHelper.is_valid_type(
                    value=dictionary.sequence_number,
                    type_callable=lambda value:
                        isinstance(
                        value,
                        int,
                ))

        if not isinstance(dictionary, dict):
            return False

        return APIHelper.is_valid_type(
                value=dictionary.get("originatingStationId"),
                type_callable=lambda value:
                    isinstance(
                    value,
                    int,
            )) \
            and APIHelper.is_valid_type(
                value=dictionary.get("sequenceNumber"),
                type_callable=lambda value:
                    isinstance(
                    value,
                    int,
            ))

    def __repr__(self):
        """Return a unambiguous string representation."""
        _originating_station_id=self.originating_station_id
        _sequence_number=self.sequence_number
        return (
            f"{self.__class__.__name__}("
            f"originating_station_id={_originating_station_id!r}, "
            f"sequence_number={_sequence_number!r}, "
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _originating_station_id=self.originating_station_id
        _sequence_number=self.sequence_number
        return (
            f"{self.__class__.__name__}("
            f"originating_station_id={_originating_station_id!s}, "
            f"sequence_number={_sequence_number!s}, "
            f")"
        )
