"""verizon.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501
from verizon.api_helper import APIHelper
from verizon.models.device_idforplanner import (
    DeviceIdforplanner,
)
from verizon.models.private_network_apns import (
    PrivateNetworkApns,
)


class DeviceListforplanner(object):
    """Implementation of the 'DeviceListforplanner' model.

    Attributes:
        device_ids (List[DeviceIdforplanner]): The model property of type
            List[DeviceIdforplanner].
        private_network_apns (List[PrivateNetworkApns]): The model property of type
            List[PrivateNetworkApns].
        ipaddress (str): A IPv4 address
        activation_code (str): The activation code value.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "device_ids": "deviceIds",
        "private_network_apns": "privateNetworkApns",
        "ipaddress": "ipAddress",
        "activation_code": "activationCode",
    }

    _optionals = [
        "device_ids",
        "private_network_apns",
        "ipaddress",
        "activation_code",
    ]

    _nullables = [
        "device_ids",
        "private_network_apns",
        "ipaddress",
        "activation_code",
    ]

    def __init__(
        self,
        device_ids=APIHelper.SKIP,
        private_network_apns=APIHelper.SKIP,
        ipaddress=APIHelper.SKIP,
        activation_code=APIHelper.SKIP):
        """Initialize a DeviceListforplanner instance."""
        # Initialize members of the class
        if device_ids is not APIHelper.SKIP:
            self.device_ids = device_ids
        if private_network_apns is not APIHelper.SKIP:
            self.private_network_apns = private_network_apns
        if ipaddress is not APIHelper.SKIP:
            self.ipaddress = ipaddress
        if activation_code is not APIHelper.SKIP:
            self.activation_code = activation_code

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        if "deviceIds" in dictionary.keys():
            device_ids = [
                DeviceIdforplanner.from_dictionary(x)
                for x in dictionary.get("deviceIds")
            ] if dictionary.get("deviceIds") else None
        else:
            device_ids = APIHelper.SKIP
        if "privateNetworkApns" in dictionary.keys():
            private_network_apns = [
                PrivateNetworkApns.from_dictionary(x)
                for x in dictionary.get("privateNetworkApns")
            ] if dictionary.get("privateNetworkApns") else None
        else:
            private_network_apns = APIHelper.SKIP
        ipaddress =\
            dictionary.get("ipAddress")\
            if "ipAddress" in dictionary.keys()\
                else APIHelper.SKIP
        activation_code =\
            dictionary.get("activationCode")\
            if "activationCode" in dictionary.keys()\
                else APIHelper.SKIP

        # Return an object of this model
        return cls(device_ids,
                   private_network_apns,
                   ipaddress,
                   activation_code)

    def __repr__(self):
        """Return a unambiguous string representation."""
        _device_ids=(
            self.device_ids
            if hasattr(self, "device_ids")
            else None
        )
        _private_network_apns=(
            self.private_network_apns
            if hasattr(self, "private_network_apns")
            else None
        )
        _ipaddress=(
            self.ipaddress
            if hasattr(self, "ipaddress")
            else None
        )
        _activation_code=(
            self.activation_code
            if hasattr(self, "activation_code")
            else None
        )
        return (
            f"{self.__class__.__name__}("
            f"device_ids={_device_ids!r}, "
            f"private_network_apns={_private_network_apns!r}, "
            f"ipaddress={_ipaddress!r}, "
            f"activation_code={_activation_code!r}, "
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _device_ids=(
            self.device_ids
            if hasattr(self, "device_ids")
            else None
        )
        _private_network_apns=(
            self.private_network_apns
            if hasattr(self, "private_network_apns")
            else None
        )
        _ipaddress=(
            self.ipaddress
            if hasattr(self, "ipaddress")
            else None
        )
        _activation_code=(
            self.activation_code
            if hasattr(self, "activation_code")
            else None
        )
        return (
            f"{self.__class__.__name__}("
            f"device_ids={_device_ids!s}, "
            f"private_network_apns={_private_network_apns!s}, "
            f"ipaddress={_ipaddress!s}, "
            f"activation_code={_activation_code!s}, "
            f")"
        )
