"""verizon.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501
from verizon.api_helper import APIHelper


class DeviceRole(object):
    """Implementation of the 'DeviceRole' model.

    The access rule (DeviceRole object) defines the topics the application or device
    can publish or subscribe to. It also defines how many parallel subscriptions one
    device or application can have and how fast it can publish messages.

    Attributes:
        name (str): The unique name of the access rule.
        subscribe_limit (int): The maximum number of subscriptions that one
            application or device can make.
        publish_rate_limit (int): The maximum rate that one application or device can
            publish messages per seconds.
        publish (List[str]): The model property of type List[str].
        subscribe (List[str]): The model property of type List[str].

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "name": "name",
        "subscribe_limit": "subscribeLimit",
        "publish_rate_limit": "publishRateLimit",
        "publish": "publish",
        "subscribe": "subscribe",
    }

    _optionals = [
        "subscribe_limit",
        "publish_rate_limit",
        "publish",
        "subscribe",
    ]

    def __init__(
        self,
        name=None,
        subscribe_limit=50,
        publish_rate_limit=15,
        publish=APIHelper.SKIP,
        subscribe=APIHelper.SKIP):
        """Initialize a DeviceRole instance."""
        # Initialize members of the class
        self.name = name
        self.subscribe_limit = subscribe_limit
        self.publish_rate_limit = publish_rate_limit
        if publish is not APIHelper.SKIP:
            self.publish = publish
        if subscribe is not APIHelper.SKIP:
            self.subscribe = subscribe

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        name =\
            dictionary.get("name")\
            if dictionary.get("name")\
                else None
        subscribe_limit =\
            dictionary.get("subscribeLimit")\
            if dictionary.get("subscribeLimit")\
                else 50
        publish_rate_limit =\
            dictionary.get("publishRateLimit")\
            if dictionary.get("publishRateLimit")\
                else 15
        publish =\
            dictionary.get("publish")\
            if dictionary.get("publish")\
                else APIHelper.SKIP
        subscribe =\
            dictionary.get("subscribe")\
            if dictionary.get("subscribe")\
                else APIHelper.SKIP

        # Return an object of this model
        return cls(name,
                   subscribe_limit,
                   publish_rate_limit,
                   publish,
                   subscribe)

    def __repr__(self):
        """Return a unambiguous string representation."""
        _name=self.name
        _subscribe_limit=(
            self.subscribe_limit
            if hasattr(self, "subscribe_limit")
            else None
        )
        _publish_rate_limit=(
            self.publish_rate_limit
            if hasattr(self, "publish_rate_limit")
            else None
        )
        _publish=(
            self.publish
            if hasattr(self, "publish")
            else None
        )
        _subscribe=(
            self.subscribe
            if hasattr(self, "subscribe")
            else None
        )
        return (
            f"{self.__class__.__name__}("
            f"name={_name!r}, "
            f"subscribe_limit={_subscribe_limit!r}, "
            f"publish_rate_limit={_publish_rate_limit!r}, "
            f"publish={_publish!r}, "
            f"subscribe={_subscribe!r}, "
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _name=self.name
        _subscribe_limit=(
            self.subscribe_limit
            if hasattr(self, "subscribe_limit")
            else None
        )
        _publish_rate_limit=(
            self.publish_rate_limit
            if hasattr(self, "publish_rate_limit")
            else None
        )
        _publish=(
            self.publish
            if hasattr(self, "publish")
            else None
        )
        _subscribe=(
            self.subscribe
            if hasattr(self, "subscribe")
            else None
        )
        return (
            f"{self.__class__.__name__}("
            f"name={_name!s}, "
            f"subscribe_limit={_subscribe_limit!s}, "
            f"publish_rate_limit={_publish_rate_limit!s}, "
            f"publish={_publish!s}, "
            f"subscribe={_subscribe!s}, "
            f")"
        )
