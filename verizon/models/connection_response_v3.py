"""verizon.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501
from verizon.api_helper import APIHelper


class ConnectionResponseV3(object):
    """Implementation of the 'ConnectionResponseV3' model.

    response for api/v3/clients/connection

    Attributes:
        mqtt_ur_ls (List[str]): Array of full MQTT URLs including protocol, host, and
            port for each available MEC.
        hosts (List[str]): Array of hostnames corresponding to each MQTT URL.
        ports (List[int]): Array of port numbers corresponding to each MQTT URL.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "mqtt_ur_ls": "MqttURLs",
        "hosts": "Hosts",
        "ports": "Ports",
    }

    _optionals = [
        "hosts",
        "ports",
    ]

    def __init__(
        self,
        mqtt_ur_ls=None,
        hosts=APIHelper.SKIP,
        ports=APIHelper.SKIP):
        """Initialize a ConnectionResponseV3 instance."""
        # Initialize members of the class
        self.mqtt_ur_ls = mqtt_ur_ls
        if hosts is not APIHelper.SKIP:
            self.hosts = hosts
        if ports is not APIHelper.SKIP:
            self.ports = ports

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        mqtt_ur_ls =\
            dictionary.get("MqttURLs")\
            if dictionary.get("MqttURLs")\
                else None
        hosts =\
            dictionary.get("Hosts")\
            if dictionary.get("Hosts")\
                else APIHelper.SKIP
        ports =\
            dictionary.get("Ports")\
            if dictionary.get("Ports")\
                else APIHelper.SKIP

        # Return an object of this model
        return cls(mqtt_ur_ls,
                   hosts,
                   ports)

    def __repr__(self):
        """Return a unambiguous string representation."""
        _mqtt_ur_ls=self.mqtt_ur_ls
        _hosts=(
            self.hosts
            if hasattr(self, "hosts")
            else None
        )
        _ports=(
            self.ports
            if hasattr(self, "ports")
            else None
        )
        return (
            f"{self.__class__.__name__}("
            f"mqtt_ur_ls={_mqtt_ur_ls!r}, "
            f"hosts={_hosts!r}, "
            f"ports={_ports!r}, "
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _mqtt_ur_ls=self.mqtt_ur_ls
        _hosts=(
            self.hosts
            if hasattr(self, "hosts")
            else None
        )
        _ports=(
            self.ports
            if hasattr(self, "ports")
            else None
        )
        return (
            f"{self.__class__.__name__}("
            f"mqtt_ur_ls={_mqtt_ur_ls!s}, "
            f"hosts={_hosts!s}, "
            f"ports={_ports!s}, "
            f")"
        )
