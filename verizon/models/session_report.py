"""verizon.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501
from verizon.api_helper import APIHelper
from verizon.models.daily_usage_item import (
    DailyUsageItem,
)


class SessionReport(object):
    """Implementation of the 'SessionReport' model.

    Session report for a device.

    Attributes:
        id (str): The 10-digit ID of the device.
        txid (str): A unique string (UUID) that associates the request with the
            location report information that is sent in asynchronous callback
            message.ThingSpace will send a separate callback message for each device
            that was in the request. All of the callback messages will have a txid.
        sessions (List[DailyUsageItem]): An object containing the start and end time
            of the session with the amount of data transferred.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "id": "id",
        "txid": "txid",
        "sessions": "sessions",
    }

    _optionals = [
        "sessions",
    ]

    def __init__(
        self,
        id=None,
        txid=None,
        sessions=APIHelper.SKIP):
        """Initialize a SessionReport instance."""
        # Initialize members of the class
        self.id = id
        self.txid = txid
        if sessions is not APIHelper.SKIP:
            self.sessions = sessions

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        id =\
            dictionary.get("id")\
            if dictionary.get("id")\
                else None
        txid =\
            dictionary.get("txid")\
            if dictionary.get("txid")\
                else None
        sessions = None
        if dictionary.get("sessions") is not None:
            sessions = [
                DailyUsageItem.from_dictionary(x)
                    for x in dictionary.get("sessions")
            ]
        else:
            sessions = APIHelper.SKIP

        # Return an object of this model
        return cls(id,
                   txid,
                   sessions)

    def __repr__(self):
        """Return a unambiguous string representation."""
        _id=self.id
        _txid=self.txid
        _sessions=(
            self.sessions
            if hasattr(self, "sessions")
            else None
        )
        return (
            f"{self.__class__.__name__}("
            f"id={_id!r}, "
            f"txid={_txid!r}, "
            f"sessions={_sessions!r}, "
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _id=self.id
        _txid=self.txid
        _sessions=(
            self.sessions
            if hasattr(self, "sessions")
            else None
        )
        return (
            f"{self.__class__.__name__}("
            f"id={_id!s}, "
            f"txid={_txid!s}, "
            f"sessions={_sessions!s}, "
            f")"
        )
