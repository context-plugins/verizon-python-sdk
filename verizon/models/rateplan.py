"""verizon.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501
from verizon.api_helper import APIHelper


class Rateplan(object):
    """Implementation of the 'rateplan' model.

    Attributes:
        rate_plan_group (List[RateplantypeObject | Rateplantype2] | None): An array
            of rate plan group names

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "rate_plan_group": "ratePlanGroup",
    }

    _optionals = [
        "rate_plan_group",
    ]

    def __init__(
        self,
        rate_plan_group=APIHelper.SKIP):
        """Initialize a Rateplan instance."""
        # Initialize members of the class
        if rate_plan_group is not APIHelper.SKIP:
            self.rate_plan_group = rate_plan_group

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        from verizon.utilities.union_type_lookup import (
            UnionTypeLookUp,
        )

        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        rate_plan_group = APIHelper.deserialize_union_type(
            UnionTypeLookUp.get("RateplanRatePlanGroup"),
            dictionary.get("ratePlanGroup"),
            False)\
            if dictionary.get("ratePlanGroup") is not None\
            else APIHelper.SKIP

        # Return an object of this model
        return cls(rate_plan_group)

    def __repr__(self):
        """Return a unambiguous string representation."""
        _rate_plan_group=(
            self.rate_plan_group
            if hasattr(self, "rate_plan_group")
            else None
        )
        return (
            f"{self.__class__.__name__}("
            f"rate_plan_group={_rate_plan_group!r}, "
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _rate_plan_group=(
            self.rate_plan_group
            if hasattr(self, "rate_plan_group")
            else None
        )
        return (
            f"{self.__class__.__name__}("
            f"rate_plan_group={_rate_plan_group!s}, "
            f")"
        )
