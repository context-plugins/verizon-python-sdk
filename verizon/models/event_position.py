"""verizon.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501
from verizon.api_helper import APIHelper
from verizon.models.altitude import Altitude
from verizon.models.pos_confidence_ellipse import (
    PosConfidenceEllipse,
)


class EventPosition(object):
    """Implementation of the 'EventPosition' model.

    Attributes:
        latitude (int): Latitude of the event location in microdegrees (900000001
            shall be used when unavailable).
        longitude (int): Longitude of the event location in microdegrees (1800000001
            shall be used when unavailable).
        position_confidence_ellipse (PosConfidenceEllipse): The model property of
            type PosConfidenceEllipse.
        altitude (Altitude): The model property of type Altitude.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "latitude": "latitude",
        "longitude": "longitude",
        "position_confidence_ellipse": "positionConfidenceEllipse",
        "altitude": "altitude",
    }

    def __init__(
        self,
        latitude=None,
        longitude=None,
        position_confidence_ellipse=None,
        altitude=None):
        """Initialize a EventPosition instance."""
        # Initialize members of the class
        self.latitude = latitude
        self.longitude = longitude
        self.position_confidence_ellipse = position_confidence_ellipse
        self.altitude = altitude

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        latitude =\
            dictionary.get("latitude")\
            if dictionary.get("latitude")\
                else None
        longitude =\
            dictionary.get("longitude")\
            if dictionary.get("longitude")\
                else None
        position_confidence_ellipse =\
            PosConfidenceEllipse.from_dictionary(
                dictionary.get("positionConfidenceEllipse"))\
                if dictionary.get("positionConfidenceEllipse") else None
        altitude =\
            Altitude.from_dictionary(
                dictionary.get("altitude"))\
                if dictionary.get("altitude") else None

        # Return an object of this model
        return cls(latitude,
                   longitude,
                   position_confidence_ellipse,
                   altitude)

    @classmethod
    def validate(cls, dictionary):
        """Validate dictionary against class required properties

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            boolean : if dictionary is valid contains required properties.

        """
        if isinstance(dictionary, cls):
            return APIHelper.is_valid_type(
                    value=dictionary.latitude,
                    type_callable=lambda value:
                        isinstance(
                        value,
                        int,
                )) \
                and APIHelper.is_valid_type(
                    value=dictionary.longitude,
                    type_callable=lambda value:
                        isinstance(
                        value,
                        int,
                )) \
                and APIHelper.is_valid_type(
                    value=dictionary.position_confidence_ellipse,
                    type_callable=lambda value:
                        PosConfidenceEllipse.validate(value),
                    is_model_dict=True) \
                and APIHelper.is_valid_type(
                    value=dictionary.altitude,
                    type_callable=lambda value:
                        Altitude.validate(value),
                    is_model_dict=True)

        if not isinstance(dictionary, dict):
            return False

        return APIHelper.is_valid_type(
                value=dictionary.get("latitude"),
                type_callable=lambda value:
                    isinstance(
                    value,
                    int,
            )) \
            and APIHelper.is_valid_type(
                value=dictionary.get("longitude"),
                type_callable=lambda value:
                    isinstance(
                    value,
                    int,
            )) \
            and APIHelper.is_valid_type(
                value=dictionary.get("positionConfidenceEllipse"),
                type_callable=lambda value:
                    PosConfidenceEllipse.validate(value),
                is_model_dict=True) \
            and APIHelper.is_valid_type(
                value=dictionary.get("altitude"),
                type_callable=lambda value:
                    Altitude.validate(value),
                is_model_dict=True)

    def __repr__(self):
        """Return a unambiguous string representation."""
        _latitude=self.latitude
        _longitude=self.longitude
        _position_confidence_ellipse=self.position_confidence_ellipse
        _altitude=self.altitude
        return (
            f"{self.__class__.__name__}("
            f"latitude={_latitude!r}, "
            f"longitude={_longitude!r}, "
            f"position_confidence_ellipse={_position_confidence_ellipse!r}, "
            f"altitude={_altitude!r}, "
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _latitude=self.latitude
        _longitude=self.longitude
        _position_confidence_ellipse=self.position_confidence_ellipse
        _altitude=self.altitude
        return (
            f"{self.__class__.__name__}("
            f"latitude={_latitude!s}, "
            f"longitude={_longitude!s}, "
            f"position_confidence_ellipse={_position_confidence_ellipse!s}, "
            f"altitude={_altitude!s}, "
            f")"
        )
