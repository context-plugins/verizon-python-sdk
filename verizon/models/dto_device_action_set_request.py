"""verizon.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501
from verizon.api_helper import APIHelper
from verizon.models.dto_device_action_set_configuration import (
    DtoDeviceActionSetConfiguration,
)
from verizon.models.dto_device_resource_identifier import (
    DtoDeviceResourceIdentifier,
)


class DtoDeviceActionSetRequest(object):
    """Implementation of the 'dto.DeviceActionSetRequest' model.

    Attributes:
        accountname (str): The numeric account name, which must include leading zeros
        configuration (DtoDeviceActionSetConfiguration): The model property of type
            DtoDeviceActionSetConfiguration.
        resourceidentifier (DtoDeviceResourceIdentifier): Device identifiers, one or
            more are required

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "accountname": "accountname",
        "configuration": "configuration",
        "resourceidentifier": "resourceidentifier",
    }

    _optionals = [
        "accountname",
        "configuration",
        "resourceidentifier",
    ]

    def __init__(
        self,
        accountname=APIHelper.SKIP,
        configuration=APIHelper.SKIP,
        resourceidentifier=APIHelper.SKIP):
        """Initialize a DtoDeviceActionSetRequest instance."""
        # Initialize members of the class
        if accountname is not APIHelper.SKIP:
            self.accountname = accountname
        if configuration is not APIHelper.SKIP:
            self.configuration = configuration
        if resourceidentifier is not APIHelper.SKIP:
            self.resourceidentifier = resourceidentifier

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        accountname =\
            dictionary.get("accountname")\
            if dictionary.get("accountname")\
                else APIHelper.SKIP
        configuration =\
            DtoDeviceActionSetConfiguration.from_dictionary(
                dictionary.get("configuration"))\
                if "configuration" in dictionary.keys()\
                else APIHelper.SKIP
        resourceidentifier =\
            DtoDeviceResourceIdentifier.from_dictionary(
                dictionary.get("resourceidentifier"))\
                if "resourceidentifier" in dictionary.keys()\
                else APIHelper.SKIP

        # Return an object of this model
        return cls(accountname,
                   configuration,
                   resourceidentifier)

    @classmethod
    def validate(cls, dictionary):
        """Validate dictionary against class required properties

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            boolean : if dictionary is valid contains required properties.

        """
        if isinstance(dictionary, cls):
            return True

        if not isinstance(dictionary, dict):
            return False

        return True

    def __repr__(self):
        """Return a unambiguous string representation."""
        _accountname=(
            self.accountname
            if hasattr(self, "accountname")
            else None
        )
        _configuration=(
            self.configuration
            if hasattr(self, "configuration")
            else None
        )
        _resourceidentifier=(
            self.resourceidentifier
            if hasattr(self, "resourceidentifier")
            else None
        )
        return (
            f"{self.__class__.__name__}("
            f"accountname={_accountname!r}, "
            f"configuration={_configuration!r}, "
            f"resourceidentifier={_resourceidentifier!r}, "
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _accountname=(
            self.accountname
            if hasattr(self, "accountname")
            else None
        )
        _configuration=(
            self.configuration
            if hasattr(self, "configuration")
            else None
        )
        _resourceidentifier=(
            self.resourceidentifier
            if hasattr(self, "resourceidentifier")
            else None
        )
        return (
            f"{self.__class__.__name__}("
            f"accountname={_accountname!s}, "
            f"configuration={_configuration!s}, "
            f"resourceidentifier={_resourceidentifier!s}, "
            f")"
        )
