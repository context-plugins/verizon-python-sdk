"""verizon.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501
from verizon.api_helper import APIHelper
from verizon.models.trigger_notification import (
    TriggerNotification,
)
from verizon.models.usage_anomaly_attributes import (
    UsageAnomalyAttributes,
)


class TriggerType2(object):
    """Implementation of the 'TriggerType2' model.

    Trigger details.

    Attributes:
        anomalyattributes (UsageAnomalyAttributes): The details of the UsageAnomaly
            trigger.
        notification (TriggerNotification): The notification details of the trigger.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "anomalyattributes": "anomalyattributes",
        "notification": "notification",
    }

    _optionals = [
        "anomalyattributes",
        "notification",
    ]

    def __init__(
        self,
        anomalyattributes=APIHelper.SKIP,
        notification=APIHelper.SKIP):
        """Initialize a TriggerType2 instance."""
        # Initialize members of the class
        if anomalyattributes is not APIHelper.SKIP:
            self.anomalyattributes = anomalyattributes
        if notification is not APIHelper.SKIP:
            self.notification = notification

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        anomalyattributes =\
            UsageAnomalyAttributes.from_dictionary(
                dictionary.get("anomalyattributes"))\
                if "anomalyattributes" in dictionary.keys()\
                else APIHelper.SKIP
        notification =\
            TriggerNotification.from_dictionary(
                dictionary.get("notification"))\
                if "notification" in dictionary.keys()\
                else APIHelper.SKIP

        # Return an object of this model
        return cls(anomalyattributes,
                   notification)

    @classmethod
    def validate(cls, dictionary):
        """Validate dictionary against class required properties

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            boolean : if dictionary is valid contains required properties.

        """
        if isinstance(dictionary, cls):
            return True

        if not isinstance(dictionary, dict):
            return False

        return True

    def __repr__(self):
        """Return a unambiguous string representation."""
        _anomalyattributes=(
            self.anomalyattributes
            if hasattr(self, "anomalyattributes")
            else None
        )
        _notification=(
            self.notification
            if hasattr(self, "notification")
            else None
        )
        return (
            f"{self.__class__.__name__}("
            f"anomalyattributes={_anomalyattributes!r}, "
            f"notification={_notification!r}, "
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _anomalyattributes=(
            self.anomalyattributes
            if hasattr(self, "anomalyattributes")
            else None
        )
        _notification=(
            self.notification
            if hasattr(self, "notification")
            else None
        )
        return (
            f"{self.__class__.__name__}("
            f"anomalyattributes={_anomalyattributes!s}, "
            f"notification={_notification!s}, "
            f")"
        )
