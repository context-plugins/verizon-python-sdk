"""verizon.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501
from verizon.api_helper import APIHelper


class ESIMGlobalDeviceList(object):
    """Implementation of the 'eSIMGlobalDeviceList' model.

    Attributes:
        account_name (str): The numeric name of the account.
        provisioning_status_filter (ProvisioningStatusFilterEnum): The last status of
            the device as a list filter.
        profile_status_filter (ProfileStatusFilterEnum): The last status of the
            device's profile as a filter.
        carrier_name_filter (str): The cellular service provider.
        device_filter (List[ESIMDeviceId | DeviceId2] | None): An array of device
            identifiers to filter the list.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "account_name": "accountName",
        "provisioning_status_filter": "provisioningStatusFilter",
        "profile_status_filter": "profileStatusFilter",
        "carrier_name_filter": "carrierNameFilter",
        "device_filter": "deviceFilter",
    }

    _optionals = [
        "account_name",
        "provisioning_status_filter",
        "profile_status_filter",
        "carrier_name_filter",
        "device_filter",
    ]

    def __init__(
        self,
        account_name=APIHelper.SKIP,
        provisioning_status_filter=APIHelper.SKIP,
        profile_status_filter=APIHelper.SKIP,
        carrier_name_filter=APIHelper.SKIP,
        device_filter=APIHelper.SKIP):
        """Initialize a ESIMGlobalDeviceList instance."""
        # Initialize members of the class
        if account_name is not APIHelper.SKIP:
            self.account_name = account_name
        if provisioning_status_filter is not APIHelper.SKIP:
            self.provisioning_status_filter = provisioning_status_filter
        if profile_status_filter is not APIHelper.SKIP:
            self.profile_status_filter = profile_status_filter
        if carrier_name_filter is not APIHelper.SKIP:
            self.carrier_name_filter = carrier_name_filter
        if device_filter is not APIHelper.SKIP:
            self.device_filter = device_filter

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        from verizon.utilities.union_type_lookup import (
            UnionTypeLookUp,
        )

        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        account_name =\
            dictionary.get("accountName")\
            if dictionary.get("accountName")\
                else APIHelper.SKIP
        provisioning_status_filter =\
            dictionary.get("provisioningStatusFilter")\
            if dictionary.get("provisioningStatusFilter")\
                else APIHelper.SKIP
        profile_status_filter =\
            dictionary.get("profileStatusFilter")\
            if dictionary.get("profileStatusFilter")\
                else APIHelper.SKIP
        carrier_name_filter =\
            dictionary.get("carrierNameFilter")\
            if dictionary.get("carrierNameFilter")\
                else APIHelper.SKIP
        device_filter = APIHelper.deserialize_union_type(
            UnionTypeLookUp.get("ESIMGlobalDeviceListDeviceFilter"),
            dictionary.get("deviceFilter"),
            False)\
            if dictionary.get("deviceFilter") is not None\
            else APIHelper.SKIP

        # Return an object of this model
        return cls(account_name,
                   provisioning_status_filter,
                   profile_status_filter,
                   carrier_name_filter,
                   device_filter)

    def __repr__(self):
        """Return a unambiguous string representation."""
        _account_name=(
            self.account_name
            if hasattr(self, "account_name")
            else None
        )
        _provisioning_status_filter=(
            self.provisioning_status_filter
            if hasattr(self, "provisioning_status_filter")
            else None
        )
        _profile_status_filter=(
            self.profile_status_filter
            if hasattr(self, "profile_status_filter")
            else None
        )
        _carrier_name_filter=(
            self.carrier_name_filter
            if hasattr(self, "carrier_name_filter")
            else None
        )
        _device_filter=(
            self.device_filter
            if hasattr(self, "device_filter")
            else None
        )
        return (
            f"{self.__class__.__name__}("
            f"account_name={_account_name!r}, "
            f"provisioning_status_filter={_provisioning_status_filter!r}, "
            f"profile_status_filter={_profile_status_filter!r}, "
            f"carrier_name_filter={_carrier_name_filter!r}, "
            f"device_filter={_device_filter!r}, "
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _account_name=(
            self.account_name
            if hasattr(self, "account_name")
            else None
        )
        _provisioning_status_filter=(
            self.provisioning_status_filter
            if hasattr(self, "provisioning_status_filter")
            else None
        )
        _profile_status_filter=(
            self.profile_status_filter
            if hasattr(self, "profile_status_filter")
            else None
        )
        _carrier_name_filter=(
            self.carrier_name_filter
            if hasattr(self, "carrier_name_filter")
            else None
        )
        _device_filter=(
            self.device_filter
            if hasattr(self, "device_filter")
            else None
        )
        return (
            f"{self.__class__.__name__}("
            f"account_name={_account_name!s}, "
            f"provisioning_status_filter={_provisioning_status_filter!s}, "
            f"profile_status_filter={_profile_status_filter!s}, "
            f"carrier_name_filter={_carrier_name_filter!s}, "
            f"device_filter={_device_filter!s}, "
            f")"
        )
