"""verizon.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501
from verizon.api_helper import APIHelper
from verizon.models.region_intersection_pair import (
    RegionIntersectionPair,
)


class ETXMAPMessageIntersectionCoordinates(object):
    """Implementation of the 'ETX MAP Message Intersection Coordinates' model.

    Query MAP records using specific region and intersection identifier pairs

    Attributes:
        message_standard (ETXMessageStandardEnum): V2X messaging standard selection.
            Accepted values are 'sae' (SAE J2735) and 'etsi' (ETSI TS 103 301).
        region_intersection_pairs (List[RegionIntersectionPair]): List of region and
            intersection ID pairs to retrieve MAP messages for.
        expected_type (ETXExpectedTypeEnum): The format of the payload in the
            response body.
        page_token (str): Base64 encoded token used to retrieve the next page of
            results
        page_size (int): Maximum number of records to return in a single page

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "region_intersection_pairs": "regionIntersectionPairs",
        "message_standard": "messageStandard",
        "expected_type": "expectedType",
        "page_token": "pageToken",
        "page_size": "pageSize",
    }

    _optionals = [
        "message_standard",
        "expected_type",
        "page_token",
        "page_size",
    ]

    def __init__(
        self,
        region_intersection_pairs=None,
        message_standard="sae",
        expected_type="BASE64",
        page_token=APIHelper.SKIP,
        page_size=200):
        """Initialize a ETXMAPMessageIntersectionCoordinates instance."""
        # Initialize members of the class
        self.message_standard = message_standard
        self.region_intersection_pairs = region_intersection_pairs
        self.expected_type = expected_type
        if page_token is not APIHelper.SKIP:
            self.page_token = page_token
        self.page_size = page_size

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        region_intersection_pairs = None
        if dictionary.get("regionIntersectionPairs") is not None:
            region_intersection_pairs = [
                RegionIntersectionPair.from_dictionary(x)
                    for x in dictionary.get("regionIntersectionPairs")
            ]
        message_standard =\
            dictionary.get("messageStandard")\
            if dictionary.get("messageStandard")\
                else "sae"
        expected_type =\
            dictionary.get("expectedType")\
            if dictionary.get("expectedType")\
                else "BASE64"
        page_token =\
            dictionary.get("pageToken")\
            if dictionary.get("pageToken")\
                else APIHelper.SKIP
        page_size =\
            dictionary.get("pageSize")\
            if dictionary.get("pageSize")\
                else 200

        # Return an object of this model
        return cls(region_intersection_pairs,
                   message_standard,
                   expected_type,
                   page_token,
                   page_size)

    @classmethod
    def validate(cls, dictionary):
        """Validate dictionary against class required properties

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            boolean : if dictionary is valid contains required properties.

        """
        if isinstance(dictionary, cls):
            return APIHelper.is_valid_type(
                    value=dictionary.region_intersection_pairs,
                    type_callable=lambda value:
                        RegionIntersectionPair.validate(value),
                    is_model_dict=True,
                    is_inner_model_dict=True)

        if not isinstance(dictionary, dict):
            return False

        return APIHelper.is_valid_type(
                value=dictionary.get("regionIntersectionPairs"),
                type_callable=lambda value:
                    RegionIntersectionPair.validate(value),
                is_model_dict=True,
                is_inner_model_dict=True)

    def __repr__(self):
        """Return a unambiguous string representation."""
        _message_standard=(
            self.message_standard
            if hasattr(self, "message_standard")
            else None
        )
        _region_intersection_pairs=self.region_intersection_pairs
        _expected_type=(
            self.expected_type
            if hasattr(self, "expected_type")
            else None
        )
        _page_token=(
            self.page_token
            if hasattr(self, "page_token")
            else None
        )
        _page_size=(
            self.page_size
            if hasattr(self, "page_size")
            else None
        )
        return (
            f"{self.__class__.__name__}("
            f"message_standard={_message_standard!r}, "
            f"region_intersection_pairs={_region_intersection_pairs!r}, "
            f"expected_type={_expected_type!r}, "
            f"page_token={_page_token!r}, "
            f"page_size={_page_size!r}, "
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _message_standard=(
            self.message_standard
            if hasattr(self, "message_standard")
            else None
        )
        _region_intersection_pairs=self.region_intersection_pairs
        _expected_type=(
            self.expected_type
            if hasattr(self, "expected_type")
            else None
        )
        _page_token=(
            self.page_token
            if hasattr(self, "page_token")
            else None
        )
        _page_size=(
            self.page_size
            if hasattr(self, "page_size")
            else None
        )
        return (
            f"{self.__class__.__name__}("
            f"message_standard={_message_standard!s}, "
            f"region_intersection_pairs={_region_intersection_pairs!s}, "
            f"expected_type={_expected_type!s}, "
            f"page_token={_page_token!s}, "
            f"page_size={_page_size!s}, "
            f")"
        )
