"""verizon.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501
from verizon.api_helper import APIHelper


class PWNProfile(object):
    """Implementation of the 'PWNProfile' model.

    Attributes:
        profile_id (str): The model property of type str.
        profile_name (str): The model property of type str.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "profile_id": "profileId",
        "profile_name": "profileName",
    }

    _optionals = [
        "profile_id",
        "profile_name",
    ]

    def __init__(
        self,
        profile_id=APIHelper.SKIP,
        profile_name=APIHelper.SKIP):
        """Initialize a PWNProfile instance."""
        # Initialize members of the class
        if profile_id is not APIHelper.SKIP:
            self.profile_id = profile_id
        if profile_name is not APIHelper.SKIP:
            self.profile_name = profile_name

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        profile_id =\
            dictionary.get("profileId")\
            if dictionary.get("profileId")\
                else APIHelper.SKIP
        profile_name =\
            dictionary.get("profileName")\
            if dictionary.get("profileName")\
                else APIHelper.SKIP

        # Return an object of this model
        return cls(profile_id,
                   profile_name)

    def __repr__(self):
        """Return a unambiguous string representation."""
        _profile_id=(
            self.profile_id
            if hasattr(self, "profile_id")
            else None
        )
        _profile_name=(
            self.profile_name
            if hasattr(self, "profile_name")
            else None
        )
        return (
            f"{self.__class__.__name__}("
            f"profile_id={_profile_id!r}, "
            f"profile_name={_profile_name!r}, "
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _profile_id=(
            self.profile_id
            if hasattr(self, "profile_id")
            else None
        )
        _profile_name=(
            self.profile_name
            if hasattr(self, "profile_name")
            else None
        )
        return (
            f"{self.__class__.__name__}("
            f"profile_id={_profile_id!s}, "
            f"profile_name={_profile_name!s}, "
            f")"
        )
