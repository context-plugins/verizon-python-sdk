"""verizon.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501
from verizon.api_helper import APIHelper


class DevicesFilter(object):
    """Implementation of the 'DevicesFilter' model.

    Optional filter criteria. Can specify one or more of:
    - ClientType: Filter devices by client type
    - ClientSubtype: Filter devices by client subtype
    - MecId: Filter devices by MEC ID
    - PageSize: Number of devices to return per page
    Valid combinations:
    - ClientType only
    - ClientSubtype only
    - ClientType and ClientSubtype together
    - MecId only
    - MecId and ClientType together
    - MecId and ClientSubtype together
    - MecId, ClientType, and ClientSubtype together
    - PageSize only
    - PageSize with any of the above combinations
    If no filter is provided, all devices for the vendor are returned.

    Attributes:
        client_type (EtxClientTypeEnum): The type of the client that is to be
            registered. This is one of the major traffic participant groups
            considered in V2X communication. The system uses this value to define
            which topics the client will be able to publish and subscribe to.
            Values: - **Vehicle** - Vehicle with an enclosure around the passengers.
            (Subtypes: Motorcycle, PassengerCar, Truck, Bus, EmergencyVehicle,
            SchoolBus, MaintenanceVehicle) - **VulnerableRoadUser** - Traffic
            participants without a protecting enclosure. (Subtypes: Bicycle,
            Pedestrian, Scooter) - **TrafficLightController** - A Traffic light
            controller system. (Subtypes: NA) - **InfrastructureSensor** - Sensors
            that are deployed in the infrastructure. (Subtypes: RoadSideUnit, Camera,
            Lidar, Radar, InductiveLoop, MagneticSensor) - **OnboardSensor** -
            Sensors that are onboard on a vehicle(Subtypes: Camera, Lidar, Radar) -
            **Software** - A software system or application. (Subtypes: Platform,
            Application, NA)
        client_subtype (ClientSubtypeEnum): The subtype or subgroup of the client
            type. This further specifies the client type. For example it will specify
            if the client is a passenger car or a truck. See the ClientType
            description for the supported Subtypes for each client type.
        mec_id (str): The unique identifier for a Multi-access Edge Computing (MEC)
            location in the ETX system. This ID is used to reference and manage MEC
            locations for registration, update, retrieval, and deletion operations.
        page_size (int): Number of devices to return per page. If not provided, the
            server default is used.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "client_type": "ClientType",
        "client_subtype": "ClientSubtype",
        "mec_id": "MecId",
        "page_size": "PageSize",
    }

    _optionals = [
        "client_type",
        "client_subtype",
        "mec_id",
        "page_size",
    ]

    def __init__(
        self,
        client_type=APIHelper.SKIP,
        client_subtype=APIHelper.SKIP,
        mec_id=APIHelper.SKIP,
        page_size=APIHelper.SKIP):
        """Initialize a DevicesFilter instance."""
        # Initialize members of the class
        if client_type is not APIHelper.SKIP:
            self.client_type = client_type
        if client_subtype is not APIHelper.SKIP:
            self.client_subtype = client_subtype
        if mec_id is not APIHelper.SKIP:
            self.mec_id = mec_id
        if page_size is not APIHelper.SKIP:
            self.page_size = page_size

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        client_type =\
            dictionary.get("ClientType")\
            if dictionary.get("ClientType")\
                else APIHelper.SKIP
        client_subtype =\
            dictionary.get("ClientSubtype")\
            if dictionary.get("ClientSubtype")\
                else APIHelper.SKIP
        mec_id =\
            dictionary.get("MecId")\
            if dictionary.get("MecId")\
                else APIHelper.SKIP
        page_size =\
            dictionary.get("PageSize")\
            if dictionary.get("PageSize")\
                else APIHelper.SKIP

        # Return an object of this model
        return cls(client_type,
                   client_subtype,
                   mec_id,
                   page_size)

    @classmethod
    def validate(cls, dictionary):
        """Validate dictionary against class required properties

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            boolean : if dictionary is valid contains required properties.

        """
        if isinstance(dictionary, cls):
            return True

        if not isinstance(dictionary, dict):
            return False

        return True

    def __repr__(self):
        """Return a unambiguous string representation."""
        _client_type=(
            self.client_type
            if hasattr(self, "client_type")
            else None
        )
        _client_subtype=(
            self.client_subtype
            if hasattr(self, "client_subtype")
            else None
        )
        _mec_id=(
            self.mec_id
            if hasattr(self, "mec_id")
            else None
        )
        _page_size=(
            self.page_size
            if hasattr(self, "page_size")
            else None
        )
        return (
            f"{self.__class__.__name__}("
            f"client_type={_client_type!r}, "
            f"client_subtype={_client_subtype!r}, "
            f"mec_id={_mec_id!r}, "
            f"page_size={_page_size!r}, "
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _client_type=(
            self.client_type
            if hasattr(self, "client_type")
            else None
        )
        _client_subtype=(
            self.client_subtype
            if hasattr(self, "client_subtype")
            else None
        )
        _mec_id=(
            self.mec_id
            if hasattr(self, "mec_id")
            else None
        )
        _page_size=(
            self.page_size
            if hasattr(self, "page_size")
            else None
        )
        return (
            f"{self.__class__.__name__}("
            f"client_type={_client_type!s}, "
            f"client_subtype={_client_subtype!s}, "
            f"mec_id={_mec_id!s}, "
            f"page_size={_page_size!s}, "
            f")"
        )
