"""verizon.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501
from verizon.api_helper import APIHelper


class RegionIntersectionPair(object):
    """Implementation of the 'RegionIntersectionPair' model.

    Specific region and intersection identification pair

    Attributes:
        region_id (int): The region identifier code (0-65535)
        intersection_id (int): The intersection identifier code (0-65535)

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "intersection_id": "intersectionId",
        "region_id": "regionId",
    }

    _optionals = [
        "region_id",
    ]

    def __init__(
        self,
        intersection_id=None,
        region_id=0):
        """Initialize a RegionIntersectionPair instance."""
        # Initialize members of the class
        self.region_id = region_id
        self.intersection_id = intersection_id

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        intersection_id =\
            dictionary.get("intersectionId")\
            if dictionary.get("intersectionId")\
                else None
        region_id =\
            dictionary.get("regionId")\
            if dictionary.get("regionId")\
                else 0

        # Return an object of this model
        return cls(intersection_id,
                   region_id)

    @classmethod
    def validate(cls, dictionary):
        """Validate dictionary against class required properties

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            boolean : if dictionary is valid contains required properties.

        """
        if isinstance(dictionary, cls):
            return APIHelper.is_valid_type(
                    value=dictionary.intersection_id,
                    type_callable=lambda value:
                        isinstance(
                        value,
                        int,
                ))

        if not isinstance(dictionary, dict):
            return False

        return APIHelper.is_valid_type(
                value=dictionary.get("intersectionId"),
                type_callable=lambda value:
                    isinstance(
                    value,
                    int,
            ))

    def __repr__(self):
        """Return a unambiguous string representation."""
        _region_id=(
            self.region_id
            if hasattr(self, "region_id")
            else None
        )
        _intersection_id=self.intersection_id
        return (
            f"{self.__class__.__name__}("
            f"region_id={_region_id!r}, "
            f"intersection_id={_intersection_id!r}, "
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _region_id=(
            self.region_id
            if hasattr(self, "region_id")
            else None
        )
        _intersection_id=self.intersection_id
        return (
            f"{self.__class__.__name__}("
            f"region_id={_region_id!s}, "
            f"intersection_id={_intersection_id!s}, "
            f")"
        )
