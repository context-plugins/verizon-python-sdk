"""verizon.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501
from verizon.api_helper import APIHelper
from verizon.models.heading_range import HeadingRange


class HeadingItem(object):
    """Implementation of the 'HeadingItem' model.

    Heading limitation provides minimum and maximum value for road user heading in
    unit of degrees. If the road user's heading value is between the given minimum
    and maximum value and the TriggerConditions are also met the message will be sent
    out.
    The heading minimum value can be bigger than the maximum value as negative number
    are not supported. For example, the +/- 10 degrees around the north (0 degrees)
    can be defined as 350 (min) to 10 (max) degrees.

    Attributes:
        heading (HeadingRange): Acceptable heading range for road users in degrees.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "heading": "heading",
    }

    _nullables = [
        "heading",
    ]

    def __init__(
        self,
        heading=None):
        """Initialize a HeadingItem instance."""
        # Initialize members of the class
        self.heading = heading

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        heading =\
            HeadingRange.from_dictionary(
                dictionary.get("heading"))\
                if dictionary.get("heading") else None

        # Return an object of this model
        return cls(heading)

    @classmethod
    def validate(cls, dictionary):
        """Validate dictionary against class required properties

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            boolean : if dictionary is valid contains required properties.

        """
        if isinstance(dictionary, cls):
            return APIHelper.is_valid_type(
                    value=dictionary.heading,
                    type_callable=lambda value:
                        HeadingRange.validate(value),
                    is_value_nullable=True,
                    is_model_dict=True)

        if not isinstance(dictionary, dict):
            return False

        return APIHelper.is_valid_type(
                value=dictionary.get("heading"),
                type_callable=lambda value:
                    HeadingRange.validate(value),
                is_value_nullable=True,
                is_model_dict=True)

    def __repr__(self):
        """Return a unambiguous string representation."""
        _heading=self.heading
        return (
            f"{self.__class__.__name__}("
            f"heading={_heading!r}, "
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _heading=self.heading
        return (
            f"{self.__class__.__name__}("
            f"heading={_heading!s}, "
            f")"
        )
