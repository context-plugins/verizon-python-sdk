"""verizon.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501
from verizon.api_helper import APIHelper
from verizon.models.action_id import ActionId
from verizon.models.event_position import EventPosition


class Management(object):
    """Implementation of the 'Management' model.

    This represent the management container describing the meta information about the
    event, such as the detection time, the event's location, the source of the event,
    and the notification distance.

    Attributes:
        action_id (ActionId): The model property of type ActionId.
        detection_time (int): Timestamp in milliseconds since start of 2004 when
            event was first generated
        reference_time (int): Timestamp in milliseconds since start of 2004 when the
            DENM message was generated.
        event_position (EventPosition): The model property of type EventPosition.
        awareness_distance (AwarenessDistanceEnum): Specifies how far the event is
            relevant to.
        station_type (int): The type of ITS station that generated the DENM. The
            value shall be set to: - 0 `unknown`          - information about the
            ITS-S context is not provided, - 1 `pedestrian`       - ITS-S carried by
            human being not using a mechanical device for their trip (VRU profile 1),
            - 2 `cyclist`          - ITS-S mounted on non-motorized unicycles,
            bicycles , tricycles, quadracycles (VRU profile 2), - 3 `moped`
            - ITS-S mounted on light motor vehicles with less than four wheels as
            defined in UNECE/TRANS/WP.29/78/Rev.4 [16]
            class L1, L2 (VRU Profile 3), - 4 `motorcycles`      - ITS-S mounted on
            motor vehicles with less than four wheels as defined in
            UNECE/TRANS/WP.29/78/Rev.4 [16]                             class L3, L4,
            L5, L6, L7 (VRU Profile 3), - 5 `passengerCar`     - ITS-S mounted on
            small passenger vehicles as defined in UNECE/TRANS/WP.29/78/Rev.4 [16]
            class M1, - 6 `bus`              - ITS-S mounted on large passenger
            vehicles as defined in UNECE/TRANS/WP.29/78/Rev.4 [16] class M2, M3, - 7
            `lightTruck`       - ITS-S mounted on light Goods Vehicles as defined in
            UNECE/TRANS/WP.29/78/Rev.4 [16] class N1, - 8 `heavyTruck`       - ITS-S
            mounted on Heavy Goods Vehicles as defined in UNECE/TRANS/WP.29/78/Rev.4
            [16] class N2 and N3, - 9 `trailer`          - ITS-S mounted on an
            unpowered vehicle that is intended to be towed by a powered vehicle as
            defined in                             UNECE/TRANS/WP.29/78/Rev.4 [16]
            class O, - 10 `specialVehicles` - ITS-S mounted on vehicles which have
            special purposes other than the above (e.g. moving road works vehicle), -
            11 `tram`            - ITS-S mounted on a vehicle which runs on tracks
            along public streets, - 12 `lightVruVehicle` - ITS-S carried by a human
            being traveling on light vehicle , incl. possible use of roller skates or
            skateboards (VRU profile 2), - 13 `animal`          - ITS-S carried by an
            animal presenting a safety risk to other road users e.g. domesticated dog
            in a city or horse (VRU Profile 4), - 14                   - reserved for
            future usage, - 15 `roadSideUnit`    - ITS-S mounted on an infrastructure
            typically positioned outside of the drivable roadway (e.g. on a gantry,
            on a pole,                             on a stationary road works
            trailer); the infrastructure is static during the entire operation period
            of the ITS-S (e.g. no stop and go activity), - 16-255               - are
            reserved for future usage.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "action_id": "actionId",
        "detection_time": "detectionTime",
        "reference_time": "referenceTime",
        "event_position": "eventPosition",
        "station_type": "stationType",
        "awareness_distance": "awarenessDistance",
    }

    _optionals = [
        "awareness_distance",
    ]

    def __init__(
        self,
        action_id=None,
        detection_time=None,
        reference_time=None,
        event_position=None,
        station_type=None,
        awareness_distance=APIHelper.SKIP):
        """Initialize a Management instance."""
        # Initialize members of the class
        self.action_id = action_id
        self.detection_time = detection_time
        self.reference_time = reference_time
        self.event_position = event_position
        if awareness_distance is not APIHelper.SKIP:
            self.awareness_distance = awareness_distance
        self.station_type = station_type

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        action_id =\
            ActionId.from_dictionary(
                dictionary.get("actionId"))\
                if dictionary.get("actionId") else None
        detection_time =\
            dictionary.get("detectionTime")\
            if dictionary.get("detectionTime")\
                else None
        reference_time =\
            dictionary.get("referenceTime")\
            if dictionary.get("referenceTime")\
                else None
        event_position =\
            EventPosition.from_dictionary(
                dictionary.get("eventPosition"))\
                if dictionary.get("eventPosition") else None
        station_type =\
            dictionary.get("stationType")\
            if dictionary.get("stationType")\
                else None
        awareness_distance =\
            dictionary.get("awarenessDistance")\
            if dictionary.get("awarenessDistance")\
                else APIHelper.SKIP

        # Return an object of this model
        return cls(action_id,
                   detection_time,
                   reference_time,
                   event_position,
                   station_type,
                   awareness_distance)

    @classmethod
    def validate(cls, dictionary):
        """Validate dictionary against class required properties

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            boolean : if dictionary is valid contains required properties.

        """
        if isinstance(dictionary, cls):
            return APIHelper.is_valid_type(
                    value=dictionary.action_id,
                    type_callable=lambda value:
                        ActionId.validate(value),
                    is_model_dict=True) \
                and APIHelper.is_valid_type(
                    value=dictionary.detection_time,
                    type_callable=lambda value:
                        isinstance(
                        value,
                        int,
                )) \
                and APIHelper.is_valid_type(
                    value=dictionary.reference_time,
                    type_callable=lambda value:
                        isinstance(
                        value,
                        int,
                )) \
                and APIHelper.is_valid_type(
                    value=dictionary.event_position,
                    type_callable=lambda value:
                        EventPosition.validate(value),
                    is_model_dict=True) \
                and APIHelper.is_valid_type(
                    value=dictionary.station_type,
                    type_callable=lambda value:
                        isinstance(
                        value,
                        int,
                ))

        if not isinstance(dictionary, dict):
            return False

        return APIHelper.is_valid_type(
                value=dictionary.get("actionId"),
                type_callable=lambda value:
                    ActionId.validate(value),
                is_model_dict=True) \
            and APIHelper.is_valid_type(
                value=dictionary.get("detectionTime"),
                type_callable=lambda value:
                    isinstance(
                    value,
                    int,
            )) \
            and APIHelper.is_valid_type(
                value=dictionary.get("referenceTime"),
                type_callable=lambda value:
                    isinstance(
                    value,
                    int,
            )) \
            and APIHelper.is_valid_type(
                value=dictionary.get("eventPosition"),
                type_callable=lambda value:
                    EventPosition.validate(value),
                is_model_dict=True) \
            and APIHelper.is_valid_type(
                value=dictionary.get("stationType"),
                type_callable=lambda value:
                    isinstance(
                    value,
                    int,
            ))

    def __repr__(self):
        """Return a unambiguous string representation."""
        _action_id=self.action_id
        _detection_time=self.detection_time
        _reference_time=self.reference_time
        _event_position=self.event_position
        _awareness_distance=(
            self.awareness_distance
            if hasattr(self, "awareness_distance")
            else None
        )
        _station_type=self.station_type
        return (
            f"{self.__class__.__name__}("
            f"action_id={_action_id!r}, "
            f"detection_time={_detection_time!r}, "
            f"reference_time={_reference_time!r}, "
            f"event_position={_event_position!r}, "
            f"awareness_distance={_awareness_distance!r}, "
            f"station_type={_station_type!r}, "
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _action_id=self.action_id
        _detection_time=self.detection_time
        _reference_time=self.reference_time
        _event_position=self.event_position
        _awareness_distance=(
            self.awareness_distance
            if hasattr(self, "awareness_distance")
            else None
        )
        _station_type=self.station_type
        return (
            f"{self.__class__.__name__}("
            f"action_id={_action_id!s}, "
            f"detection_time={_detection_time!s}, "
            f"reference_time={_reference_time!s}, "
            f"event_position={_event_position!s}, "
            f"awareness_distance={_awareness_distance!s}, "
            f"station_type={_station_type!s}, "
            f")"
        )
