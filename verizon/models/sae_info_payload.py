"""verizon.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501
from verizon.api_helper import APIHelper
from verizon.models.data_frame import DataFrame


class SaeInfoPayload(object):
    """Implementation of the 'SaeInfoPayload' model.

    Traveler Information Message (TIM) payload as defined in SAE J2735.

    Attributes:
        msg_cnt (int): It is used to provide a sequence number within a stream of
            messages with the same DSRCmsgID (here RoadSideAlert) and from the same
            sender.
        time_stamp (int): The number of elapsed minutes of the current year in the
            time system being used (typically UTC time). -- the value 527040 shall be
            used for invalid
        packet_id (str): Provides a relatively unique value which can be used to
            connect to (link to) other supporting messages in other formats.  The
            value is described as a 18-character hexadecimal string.
        url_b (str): A valid internet style URI/URL in the form of a text string
            which will form the base of a compound string which, when combined with
            the URL-short data element, will link to the designated resource.
        data_frames (List[DataFrame]): List of data frames.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "data_frames": "dataFrames",
        "msg_cnt": "msgCnt",
        "time_stamp": "timeStamp",
        "packet_id": "packetID",
        "url_b": "urlB",
    }

    _optionals = [
        "msg_cnt",
        "time_stamp",
        "packet_id",
        "url_b",
    ]

    def __init__(
        self,
        data_frames=None,
        msg_cnt=0,
        time_stamp=APIHelper.SKIP,
        packet_id=APIHelper.SKIP,
        url_b=APIHelper.SKIP):
        """Initialize a SaeInfoPayload instance."""
        # Initialize members of the class
        self.msg_cnt = msg_cnt
        if time_stamp is not APIHelper.SKIP:
            self.time_stamp = time_stamp
        if packet_id is not APIHelper.SKIP:
            self.packet_id = packet_id
        if url_b is not APIHelper.SKIP:
            self.url_b = url_b
        self.data_frames = data_frames

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        data_frames = None
        if dictionary.get("dataFrames") is not None:
            data_frames = [
                DataFrame.from_dictionary(x)
                    for x in dictionary.get("dataFrames")
            ]
        msg_cnt =\
            dictionary.get("msgCnt")\
            if dictionary.get("msgCnt")\
                else 0
        time_stamp =\
            dictionary.get("timeStamp")\
            if dictionary.get("timeStamp")\
                else APIHelper.SKIP
        packet_id =\
            dictionary.get("packetID")\
            if dictionary.get("packetID")\
                else APIHelper.SKIP
        url_b =\
            dictionary.get("urlB")\
            if dictionary.get("urlB")\
                else APIHelper.SKIP

        # Return an object of this model
        return cls(data_frames,
                   msg_cnt,
                   time_stamp,
                   packet_id,
                   url_b)

    @classmethod
    def validate(cls, dictionary):
        """Validate dictionary against class required properties

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            boolean : if dictionary is valid contains required properties.

        """
        if isinstance(dictionary, cls):
            return APIHelper.is_valid_type(
                    value=dictionary.data_frames,
                    type_callable=lambda value:
                        DataFrame.validate(value),
                    is_model_dict=True,
                    is_inner_model_dict=True)

        if not isinstance(dictionary, dict):
            return False

        return APIHelper.is_valid_type(
                value=dictionary.get("dataFrames"),
                type_callable=lambda value:
                    DataFrame.validate(value),
                is_model_dict=True,
                is_inner_model_dict=True)

    def __repr__(self):
        """Return a unambiguous string representation."""
        _msg_cnt=(
            self.msg_cnt
            if hasattr(self, "msg_cnt")
            else None
        )
        _time_stamp=(
            self.time_stamp
            if hasattr(self, "time_stamp")
            else None
        )
        _packet_id=(
            self.packet_id
            if hasattr(self, "packet_id")
            else None
        )
        _url_b=(
            self.url_b
            if hasattr(self, "url_b")
            else None
        )
        _data_frames=self.data_frames
        return (
            f"{self.__class__.__name__}("
            f"msg_cnt={_msg_cnt!r}, "
            f"time_stamp={_time_stamp!r}, "
            f"packet_id={_packet_id!r}, "
            f"url_b={_url_b!r}, "
            f"data_frames={_data_frames!r}, "
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _msg_cnt=(
            self.msg_cnt
            if hasattr(self, "msg_cnt")
            else None
        )
        _time_stamp=(
            self.time_stamp
            if hasattr(self, "time_stamp")
            else None
        )
        _packet_id=(
            self.packet_id
            if hasattr(self, "packet_id")
            else None
        )
        _url_b=(
            self.url_b
            if hasattr(self, "url_b")
            else None
        )
        _data_frames=self.data_frames
        return (
            f"{self.__class__.__name__}("
            f"msg_cnt={_msg_cnt!s}, "
            f"time_stamp={_time_stamp!s}, "
            f"packet_id={_packet_id!s}, "
            f"url_b={_url_b!s}, "
            f"data_frames={_data_frames!s}, "
            f")"
        )
