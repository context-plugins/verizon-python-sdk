"""verizon.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501
from verizon.api_helper import APIHelper
from verizon.models.geo_fence import GeoFence


class GeoFenceConfigurationRequest(object):
    """Implementation of the 'GeoFenceConfigurationRequest' model.

    Request for /api/v1/application/configurations/geofence POST endpoint. It
    requires the vendorId, geofence, messageStandard, messages and isActive fields to
    be populated.

    Attributes:
        name (str): Name of the configuration.
        description (str): Description of the configuration.
        geo_fence (GeoFence): The GeoJSON representation of geofence. Geofence
            supports the following geometry types: LineString, Polygon,
            MultiLineString, and MultiPolygon. The system only supports a single
            Feature in the FeatureCollection, so only one Line, Polygon, MultiLine or
            MultiPolygon can be defined within one Geofencing configuration.
        message_standard (MessageStandardEnum): Select which V2X messaging standard
            will be used for the message generation. The following options are
            supported:   - "etsi": The message will be generated using the ETSI
            (European) standard (e.g. DENM).   - "sae": The message will be generated
            using the SAE J2735 (North American) standard (e.g. RSA, TIM).   - if not
            sent while POST, defaults to "sae"   - mandatory to send "etsi" standard
            here, if ETSI messages are being sent in config
        messages (List[Message | Message1 | Message2 | Message3]): List of predefined
            messages that belongs to the geofence. These are the messages that are
            sent out by the system when the Trigger Condition for the message is met.
        is_active (bool): The model property of type bool.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "geo_fence": "geoFence",
        "messages": "messages",
        "is_active": "isActive",
        "name": "name",
        "description": "description",
        "message_standard": "messageStandard",
    }

    _optionals = [
        "name",
        "description",
        "message_standard",
    ]

    def __init__(
        self,
        geo_fence=None,
        messages=None,
        is_active=None,
        name=APIHelper.SKIP,
        description=APIHelper.SKIP,
        message_standard="sae"):
        """Initialize a GeoFenceConfigurationRequest instance."""
        # Initialize members of the class
        if name is not APIHelper.SKIP:
            self.name = name
        if description is not APIHelper.SKIP:
            self.description = description
        self.geo_fence = geo_fence
        self.message_standard = message_standard
        self.messages = messages
        self.is_active = is_active

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        from verizon.utilities.union_type_lookup import (
            UnionTypeLookUp,
        )

        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        geo_fence =\
            GeoFence.from_dictionary(
                dictionary.get("geoFence"))\
                if dictionary.get("geoFence") else None
        messages = APIHelper.deserialize_union_type(
            UnionTypeLookUp.get("Message4"),
            dictionary.get("messages"),
            False)\
            if dictionary.get("messages") is not None\
            else None
        is_active =\
            dictionary.get("isActive")\
            if "isActive" in dictionary.keys()\
                else None
        name =\
            dictionary.get("name")\
            if dictionary.get("name")\
                else APIHelper.SKIP
        description =\
            dictionary.get("description")\
            if dictionary.get("description")\
                else APIHelper.SKIP
        message_standard =\
            dictionary.get("messageStandard")\
            if dictionary.get("messageStandard")\
                else "sae"

        # Return an object of this model
        return cls(geo_fence,
                   messages,
                   is_active,
                   name,
                   description,
                   message_standard)

    @classmethod
    def validate(cls, dictionary):
        """Validate dictionary against class required properties

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            boolean : if dictionary is valid contains required properties.

        """
        from verizon.utilities.union_type_lookup import (
            UnionTypeLookUp,
        )

        if isinstance(dictionary, cls):
            return APIHelper.is_valid_type(
                    value=dictionary.geo_fence,
                    type_callable=lambda value:
                        GeoFence.validate(value),
                    is_model_dict=True) \
                and (UnionTypeLookUp.get("Message4")
                .validate(dictionary.messages).is_valid) \
                and APIHelper.is_valid_type(
                    value=dictionary.is_active,
                    type_callable=lambda value:
                        isinstance(
                        value,
                        bool,
                ))

        if not isinstance(dictionary, dict):
            return False

        return APIHelper.is_valid_type(
                value=dictionary.get("geoFence"),
                type_callable=lambda value:
                    GeoFence.validate(value),
                is_model_dict=True) \
            and (UnionTypeLookUp.get("Message4")
            .validate(dictionary.get("messages")).is_valid) \
            and APIHelper.is_valid_type(
                value=dictionary.get("isActive"),
                type_callable=lambda value:
                    isinstance(
                    value,
                    bool,
            ))

    def __repr__(self):
        """Return a unambiguous string representation."""
        _name=(
            self.name
            if hasattr(self, "name")
            else None
        )
        _description=(
            self.description
            if hasattr(self, "description")
            else None
        )
        _geo_fence=self.geo_fence
        _message_standard=(
            self.message_standard
            if hasattr(self, "message_standard")
            else None
        )
        _messages=self.messages
        _is_active=self.is_active
        return (
            f"{self.__class__.__name__}("
            f"name={_name!r}, "
            f"description={_description!r}, "
            f"geo_fence={_geo_fence!r}, "
            f"message_standard={_message_standard!r}, "
            f"messages={_messages!r}, "
            f"is_active={_is_active!r}, "
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _name=(
            self.name
            if hasattr(self, "name")
            else None
        )
        _description=(
            self.description
            if hasattr(self, "description")
            else None
        )
        _geo_fence=self.geo_fence
        _message_standard=(
            self.message_standard
            if hasattr(self, "message_standard")
            else None
        )
        _messages=self.messages
        _is_active=self.is_active
        return (
            f"{self.__class__.__name__}("
            f"name={_name!s}, "
            f"description={_description!s}, "
            f"geo_fence={_geo_fence!s}, "
            f"message_standard={_message_standard!s}, "
            f"messages={_messages!s}, "
            f"is_active={_is_active!s}, "
            f")"
        )
