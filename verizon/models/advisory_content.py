"""verizon.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501
from verizon.api_helper import APIHelper


class AdvisoryContent(object):
    """Implementation of the 'AdvisoryContent' model.

    DataFrame content variant carrying advisory ITIS codes.

    Attributes:
        advisory (List[ITISItemWrapper | TextItemWrapper]): List of typical ITIS
            warnings.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "advisory": "advisory",
    }

    def __init__(
        self,
        advisory=None):
        """Initialize a AdvisoryContent instance."""
        # Initialize members of the class
        self.advisory = advisory

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        from verizon.utilities.union_type_lookup import (
            UnionTypeLookUp,
        )

        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        advisory = APIHelper.deserialize_union_type(
            UnionTypeLookUp.get("AdvisoryItem"),
            dictionary.get("advisory"),
            False)\
            if dictionary.get("advisory") is not None\
            else None

        # Return an object of this model
        return cls(advisory)

    @classmethod
    def validate(cls, dictionary):
        """Validate dictionary against class required properties

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            boolean : if dictionary is valid contains required properties.

        """
        from verizon.utilities.union_type_lookup import (
            UnionTypeLookUp,
        )

        if isinstance(dictionary, cls):
            return (UnionTypeLookUp.get("AdvisoryItem")
                .validate(dictionary.advisory).is_valid)

        if not isinstance(dictionary, dict):
            return False

        return (UnionTypeLookUp.get("AdvisoryItem")
            .validate(dictionary.get("advisory")).is_valid)

    def __repr__(self):
        """Return a unambiguous string representation."""
        _advisory=self.advisory
        return (
            f"{self.__class__.__name__}("
            f"advisory={_advisory!r}, "
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _advisory=self.advisory
        return (
            f"{self.__class__.__name__}("
            f"advisory={_advisory!s}, "
            f")"
        )
