"""verizon.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501
from verizon.api_helper import APIHelper


class PosConfidenceEllipse(object):
    """Implementation of the 'PosConfidenceEllipse' model.

    Attributes:
        semi_major_confidence (int): Absolute position accuracy in one of the axis
            direction as defined in a shape of ellipse with a predefined confidence
            level (set to 4095 when unavailable). The value shall be set to: - `n`
            (`n > 0` and `n < 4094`) if the accuracy is equal to or less than n *
            0,01 metre, - `4094` if the accuracy is out of range, i.e. greater than
            4,093 m, - `4095` if the accuracy information is unavailable. The value 0
            shall not be used.
        semi_minor_confidence (int): Absolute position accuracy in one of the axis
            direction as defined in a shape of ellipse with a predefined confidence
            level (set to 4095 when unavailable). The value shall be set to: - `n`
            (`n > 0` and `n < 4094`) if the accuracy is equal to or less than n *
            0,01 metre, - `4094` if the accuracy is out of range, i.e. greater than
            4,093 m, - `4095` if the accuracy information is unavailable. The value 0
            shall not be used.
        semi_major_orientation (int): An angle value in degrees described in the
            WGS84 reference system with respect to the WGS84 north. The value shall
            be set to: - wgs84North  (0), - wgs84East   (900), - wgs84South  (1800),
            - wgs84West   (2700), - doNotUse    (3600), - unavailable (3601)

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "semi_major_confidence": "semiMajorConfidence",
        "semi_minor_confidence": "semiMinorConfidence",
        "semi_major_orientation": "semiMajorOrientation",
    }

    def __init__(
        self,
        semi_major_confidence=None,
        semi_minor_confidence=None,
        semi_major_orientation=None):
        """Initialize a PosConfidenceEllipse instance."""
        # Initialize members of the class
        self.semi_major_confidence = semi_major_confidence
        self.semi_minor_confidence = semi_minor_confidence
        self.semi_major_orientation = semi_major_orientation

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        semi_major_confidence =\
            dictionary.get("semiMajorConfidence")\
            if dictionary.get("semiMajorConfidence")\
                else None
        semi_minor_confidence =\
            dictionary.get("semiMinorConfidence")\
            if dictionary.get("semiMinorConfidence")\
                else None
        semi_major_orientation =\
            dictionary.get("semiMajorOrientation")\
            if dictionary.get("semiMajorOrientation")\
                else None

        # Return an object of this model
        return cls(semi_major_confidence,
                   semi_minor_confidence,
                   semi_major_orientation)

    @classmethod
    def validate(cls, dictionary):
        """Validate dictionary against class required properties

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            boolean : if dictionary is valid contains required properties.

        """
        if isinstance(dictionary, cls):
            return APIHelper.is_valid_type(
                    value=dictionary.semi_major_confidence,
                    type_callable=lambda value:
                        isinstance(
                        value,
                        int,
                )) \
                and APIHelper.is_valid_type(
                    value=dictionary.semi_minor_confidence,
                    type_callable=lambda value:
                        isinstance(
                        value,
                        int,
                )) \
                and APIHelper.is_valid_type(
                    value=dictionary.semi_major_orientation,
                    type_callable=lambda value:
                        isinstance(
                        value,
                        int,
                ))

        if not isinstance(dictionary, dict):
            return False

        return APIHelper.is_valid_type(
                value=dictionary.get("semiMajorConfidence"),
                type_callable=lambda value:
                    isinstance(
                    value,
                    int,
            )) \
            and APIHelper.is_valid_type(
                value=dictionary.get("semiMinorConfidence"),
                type_callable=lambda value:
                    isinstance(
                    value,
                    int,
            )) \
            and APIHelper.is_valid_type(
                value=dictionary.get("semiMajorOrientation"),
                type_callable=lambda value:
                    isinstance(
                    value,
                    int,
            ))

    def __repr__(self):
        """Return a unambiguous string representation."""
        _semi_major_confidence=self.semi_major_confidence
        _semi_minor_confidence=self.semi_minor_confidence
        _semi_major_orientation=self.semi_major_orientation
        return (
            f"{self.__class__.__name__}("
            f"semi_major_confidence={_semi_major_confidence!r}, "
            f"semi_minor_confidence={_semi_minor_confidence!r}, "
            f"semi_major_orientation={_semi_major_orientation!r}, "
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _semi_major_confidence=self.semi_major_confidence
        _semi_minor_confidence=self.semi_minor_confidence
        _semi_major_orientation=self.semi_major_orientation
        return (
            f"{self.__class__.__name__}("
            f"semi_major_confidence={_semi_major_confidence!s}, "
            f"semi_minor_confidence={_semi_minor_confidence!s}, "
            f"semi_major_orientation={_semi_major_orientation!s}, "
            f")"
        )
