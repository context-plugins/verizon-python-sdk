"""verizon.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501
from verizon.api_helper import APIHelper
from verizon.models.type_2_enum import Type2Enum


class LineString(object):
    """Implementation of the 'LineString' model.

    A LineString is a type of geometry that represents a collection of points that
    are connected by line segments.

    Attributes:
        mtype (Type2Enum): The model property of type Type2Enum.
        coordinates (List[float]): The model property of type List[float].

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "mtype": "type",
        "coordinates": "coordinates",
    }

    def __init__(
        self,
        mtype=None,
        coordinates=None):
        """Initialize a LineString instance."""
        # Initialize members of the class
        self.mtype = mtype
        self.coordinates = coordinates

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        mtype =\
            dictionary.get("type")\
            if dictionary.get("type")\
                else None
        coordinates =\
            dictionary.get("coordinates")\
            if dictionary.get("coordinates")\
                else None

        # Return an object of this model
        return cls(mtype,
                   coordinates)

    @classmethod
    def validate(cls, dictionary):
        """Validate dictionary against class required properties

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            boolean : if dictionary is valid contains required properties.

        """
        if isinstance(dictionary, cls):
            return APIHelper.is_valid_type(
                    value=dictionary.mtype,
                    type_callable=lambda value:
                        Type2Enum.validate(value)) \
                and APIHelper.is_valid_type(
                    value=dictionary.coordinates,
                    type_callable=lambda value:
                        isinstance(
                        value,
                        float,
                ))

        if not isinstance(dictionary, dict):
            return False

        return APIHelper.is_valid_type(
                value=dictionary.get("type"),
                type_callable=lambda value:
                    Type2Enum.validate(value)) \
            and APIHelper.is_valid_type(
                value=dictionary.get("coordinates"),
                type_callable=lambda value:
                    isinstance(
                    value,
                    float,
            ))

    def __repr__(self):
        """Return a unambiguous string representation."""
        _mtype=self.mtype
        _coordinates=self.coordinates
        return (
            f"{self.__class__.__name__}("
            f"mtype={_mtype!r}, "
            f"coordinates={_coordinates!r}, "
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _mtype=self.mtype
        _coordinates=self.coordinates
        return (
            f"{self.__class__.__name__}("
            f"mtype={_mtype!s}, "
            f"coordinates={_coordinates!s}, "
            f")"
        )
