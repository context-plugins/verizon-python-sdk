"""verizon.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501
from verizon.api_helper import APIHelper
from verizon.models.kv_pairforplanner import (
    KvPairforplanner,
)


class ServicePlanResponseforplanner(object):
    """Implementation of the 'ServicePlanResponseforplanner' model.

    Attributes:
        carrier_service_plan_code (str): The name of the service plan code
        code (str): The actiavtion code value.
        extended_attributes (List[KvPairforplanner]): key/value pairs assigned by the
            user for filtering.
        name (str): The carrier name of the active profile.
        size_kb (int): size in Kilobytes of the service plan

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "carrier_service_plan_code": "carrierServicePlanCode",
        "code": "code",
        "extended_attributes": "extendedAttributes",
        "name": "name",
        "size_kb": "sizeKb",
    }

    _optionals = [
        "carrier_service_plan_code",
        "code",
        "extended_attributes",
        "name",
        "size_kb",
    ]

    def __init__(
        self,
        carrier_service_plan_code=APIHelper.SKIP,
        code=APIHelper.SKIP,
        extended_attributes=APIHelper.SKIP,
        name=APIHelper.SKIP,
        size_kb=APIHelper.SKIP):
        """Initialize a ServicePlanResponseforplanner instance."""
        # Initialize members of the class
        if carrier_service_plan_code is not APIHelper.SKIP:
            self.carrier_service_plan_code = carrier_service_plan_code
        if code is not APIHelper.SKIP:
            self.code = code
        if extended_attributes is not APIHelper.SKIP:
            self.extended_attributes = extended_attributes
        if name is not APIHelper.SKIP:
            self.name = name
        if size_kb is not APIHelper.SKIP:
            self.size_kb = size_kb

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        carrier_service_plan_code =\
            dictionary.get("carrierServicePlanCode")\
            if dictionary.get("carrierServicePlanCode")\
                else APIHelper.SKIP
        code =\
            dictionary.get("code")\
            if dictionary.get("code")\
                else APIHelper.SKIP
        extended_attributes = None
        if dictionary.get("extendedAttributes") is not None:
            extended_attributes = [
                KvPairforplanner.from_dictionary(x)
                    for x in dictionary.get("extendedAttributes")
            ]
        else:
            extended_attributes = APIHelper.SKIP
        name =\
            dictionary.get("name")\
            if dictionary.get("name")\
                else APIHelper.SKIP
        size_kb =\
            dictionary.get("sizeKb")\
            if dictionary.get("sizeKb")\
                else APIHelper.SKIP

        # Return an object of this model
        return cls(carrier_service_plan_code,
                   code,
                   extended_attributes,
                   name,
                   size_kb)

    def __repr__(self):
        """Return a unambiguous string representation."""
        _carrier_service_plan_code=(
            self.carrier_service_plan_code
            if hasattr(self, "carrier_service_plan_code")
            else None
        )
        _code=(
            self.code
            if hasattr(self, "code")
            else None
        )
        _extended_attributes=(
            self.extended_attributes
            if hasattr(self, "extended_attributes")
            else None
        )
        _name=(
            self.name
            if hasattr(self, "name")
            else None
        )
        _size_kb=(
            self.size_kb
            if hasattr(self, "size_kb")
            else None
        )
        return (
            f"{self.__class__.__name__}("
            f"carrier_service_plan_code={_carrier_service_plan_code!r}, "
            f"code={_code!r}, "
            f"extended_attributes={_extended_attributes!r}, "
            f"name={_name!r}, "
            f"size_kb={_size_kb!r}, "
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _carrier_service_plan_code=(
            self.carrier_service_plan_code
            if hasattr(self, "carrier_service_plan_code")
            else None
        )
        _code=(
            self.code
            if hasattr(self, "code")
            else None
        )
        _extended_attributes=(
            self.extended_attributes
            if hasattr(self, "extended_attributes")
            else None
        )
        _name=(
            self.name
            if hasattr(self, "name")
            else None
        )
        _size_kb=(
            self.size_kb
            if hasattr(self, "size_kb")
            else None
        )
        return (
            f"{self.__class__.__name__}("
            f"carrier_service_plan_code={_carrier_service_plan_code!s}, "
            f"code={_code!s}, "
            f"extended_attributes={_extended_attributes!s}, "
            f"name={_name!s}, "
            f"size_kb={_size_kb!s}, "
            f")"
        )
