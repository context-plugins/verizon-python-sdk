"""verizon.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501
from verizon.api_helper import APIHelper
from verizon.models.node_ll import NodeLL


class NodeListLL(object):
    """Implementation of the 'NodeListLL' model.

    The NodeListLL data structure provides the sequence of signed offset node point
    values for determining the latitude and longitude. Each LL point is referred to
    as a node point.

    Attributes:
        nodes (List[NodeLL]): The NodeSetLL data frame consists of a list of NodeLL
            entries using LL offsets.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "nodes": "nodes",
    }

    def __init__(
        self,
        nodes=None):
        """Initialize a NodeListLL instance."""
        # Initialize members of the class
        self.nodes = nodes

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        nodes = None
        if dictionary.get("nodes") is not None:
            nodes = [
                NodeLL.from_dictionary(x)
                    for x in dictionary.get("nodes")
            ]

        # Return an object of this model
        return cls(nodes)

    @classmethod
    def validate(cls, dictionary):
        """Validate dictionary against class required properties

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            boolean : if dictionary is valid contains required properties.

        """
        if isinstance(dictionary, cls):
            return APIHelper.is_valid_type(
                    value=dictionary.nodes,
                    type_callable=lambda value:
                        NodeLL.validate(value),
                    is_model_dict=True,
                    is_inner_model_dict=True)

        if not isinstance(dictionary, dict):
            return False

        return APIHelper.is_valid_type(
                value=dictionary.get("nodes"),
                type_callable=lambda value:
                    NodeLL.validate(value),
                is_model_dict=True,
                is_inner_model_dict=True)

    def __repr__(self):
        """Return a unambiguous string representation."""
        _nodes=self.nodes
        return (
            f"{self.__class__.__name__}("
            f"nodes={_nodes!r}, "
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _nodes=self.nodes
        return (
            f"{self.__class__.__name__}("
            f"nodes={_nodes!s}, "
            f")"
        )
