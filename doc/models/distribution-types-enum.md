
# Distribution Types Enum

The distribution types:

- Targeted: Generate targeted messages to the road users that are affected by the zone rules
- Broadcast: Broadcast messages to regions based on the Geofence.

## Enumeration

`DistributionTypesEnum`

## Fields

| Name |
|  --- |
| `TARGETED` |
| `BROADCAST` |

## Example

```python
from verizon.models.distribution_types_enum import DistributionTypesEnum

distribution_types = DistributionTypesEnum.TARGETED
```

